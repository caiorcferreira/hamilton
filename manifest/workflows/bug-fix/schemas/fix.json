{
  "type": "object",
  "required": ["status"],
  "properties": {
    "status": { "type": "string", "enum": ["done", "failed"] },
    "changes": { "type": "string" },
    "tests": { "type": "string" }
  }
}
