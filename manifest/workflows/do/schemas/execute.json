{
  "type": "object",
  "required": ["status"],
  "properties": {
    "status": { "type": "string", "enum": ["done", "failed"] },
    "result": { "type": "string" },
    "changes": { "type": "string" }
  }
}
