# Phase 02: Refactor propose and critique onto hamilton-grilling (Route Unit 7)

Refactor `hamilton-propose` and `hamilton-critique` so their one-question-at-a-time dialogue delegates to `hamilton-grilling` instead of being embedded inline. Propose delegates at all three dialogue surfaces (step 4 clarifying questions, step 7 approach choice, step 10 approval loop); critique — which today has no dialogue at all — gains grilling on the `changes-requested` path only, positioned between the code-quality rubric and writing the report so findings are validated before `critique.md` exists. The main move is *removal* of propose's inline dialogue, which is where skills rot, so the craft focus is what the deletions leave behind. Uses the **oracle** agent for the edits to two of the repo's most-invoked skills (retries capped at 2). Runs the full SDD loop and must serialize after Phase 01 only in that both touch skill files — propose is untouched by Phase 01, so the only ordering constraint is that grilling (unit 4, already shipped) exists.

## Tasks

- [x] Load the unit's context. Read the `### 7. Refactor propose and critique onto hamilton-grilling` section of `.hamilton/maps/hamilton-wayfinder/route.md` and its single backing ticket `tickets/12-propose-and-critique-use-grilling.md`. Then read the current `skills/hamilton-propose/SKILL.md` (steps 4, 7, and 10 carry the inline dialogue to extract), `skills/hamilton-critique/SKILL.md` (note it has no dialogue today — ticket 12 corrects ticket 07's premise on this), and `skills/hamilton-grilling/SKILL.md` (the protocol primitive the callers will delegate to). Also read the route's craft-focus paragraph for this unit on what removal leaves behind.

  **Context loaded — 0 images analyzed.** Key findings:

  - **Unit 7 status:** `pending`, depends on unit 4 (grilling, shipped). Units 1–6 all shipped; unit 7 is the next open unit.
  - **Ticket 12 boundary:** Grilling owns the **protocol only** (one question at a time, lead with a recommendation, look facts up, never answer for the human). Callers own **question content** and the **exit condition**. Every protocol sentence left in propose after the edit is duplication.
  - **Propose — 3 inline dialogue surfaces to extract:** Step 4 (clarifying questions — "one question at a time, multiple-choice when you can"), step 7 (approach choice — "lead with your recommendation… get the requester's choice"), step 10 (approval loop — "revise and re-review on request"). Also the **Principles** section (line 46–47) carries the inline protocol ("ask one question at a time, prefer multiple-choice") and the **process flow diagram** labels step 4 as "(one at a time)".
  - **Critique — no dialogue today (confirms ticket 12's correction of ticket 07):** Process is step 5 (verdict) → step 6 (write report). Grilling inserts on the `changes-requested` path **between** the rubric (step 4) and the report write (step 6), so findings are validated before `critique.md` exists. "Judge, don't fix" survives — critique still never edits artifacts; the revision loop stays with whoever runs the pipeline.
  - **Attendance guard:** At each call site — attended → invoke `hamilton-grilling`; unattended → fall back to the behavior both skills already document (propose records a reasonable choice as an assumption; critique names the next step and returns). Grilling itself never gains an unattended mode.
  - **Craft focus (route.md lines 213–220):** "What the deletions leave behind." Main move is *removal*. Removal is where skills rot: leftover half-instructions, steps that read as non-sequiturs, duplication between what propose still says about dialogue and what grilling now owns. Read edited steps end-to-end afterwards, not just the diff.
  - **No test coverage needed** — `skills/` is not bundled; no test asserts on skill content. Gates (`bun run build`, `bun --bun vitest run`) must stay green.

- [x] Read `/writing-great-skills` (SKILL.md at `~/.agents/skills/writing-great-skills/SKILL.md`) **before editing** — it is `disable-model-invocation: true`, read it explicitly. Focus on the **duplication** and **sediment** failure modes: every protocol sentence left in propose after this edit is duplication (grilling now owns the protocol), and leftover half-instructions are the rot the route warns about.

  **Read complete — 0 images analyzed.** Read both `SKILL.md` and its disclosed `GLOSSARY.md`. Key takeaways that will govern the propose/critique edits:

  - **Duplication** (_failure mode_, glossary L177–181): the same meaning in more than one single source of truth. Costs maintenance, tokens, and inflates a meaning's prominence on the ladder past its real rank. Applied here: once `hamilton-grilling` owns the dialogue protocol (one question at a time, lead with a recommendation, look facts up rather than asking, never answer for the human), **every protocol sentence left in propose is duplication** — a second source of truth that will drift the moment grilling's protocol evolves. The end state is one authoritative place for the protocol; propose keeps only the *call* (delegate to grilling) plus the *question content* and *exit condition* the ticket assigns to the caller.
  - **Sediment** (_failure mode_, glossary L189–193): stale layers that settle because adding feels safe and removing feels risky; the slow erosion of relevance. This is the rot the route's craft-focus paragraph warns about. After the dialogue is extracted, propose must be read **end-to-end** — any half-instruction that referenced the now-removed protocol (e.g. "ask one question at a time, prefer multiple-choice" surviving in the Principles section, or the process-flow diagram still labelling step 4 "(one at a time)") is sediment and must go, not be trimmed around.
  - **No-op** (_failure mode_, glossary L196–201): an instruction that changes nothing because the model already does it by default. Post-delegation, any residual "be concise / one at a time / don't answer for the human" line in propose becomes a no-op *and* a duplication — grilling already enforces it. The no-op test is run **sentence by sentence**, not line by line; when a sentence fails, delete the whole sentence, don't word-trim it.
  - **Single source of truth** (glossary L171–175): the desired end state — each meaning in exactly one authoritative place. Duplication is its violation. This is the bar the review (later task) checks against: read edited steps end-to-end and confirm no protocol meaning lives in two places.
  - **Pruning discipline** (SKILL.md L53–59): keep each meaning in one place → check every line for relevance → hunt no-ops sentence by sentence. The order matters: relevance first (does this line still bear on what propose does *after* delegation?), then the no-op test on each surviving sentence. Be aggressive — most prose that fails should go, not be rewritten.
  - **Negation** caution (glossary L162–165): if propose's edited steps need to say what *not* to do, phrase the positive target instead. Relevant because the attendance guard ("unattended → fall back, don't invoke grilling") is cleaner as a positive branch ("unattended → record a reasonable choice as an assumption") than a prohibition.
  - **Leading words** (SKILL.md L61–72): the refactor is a chance to collapse any restated triads into a single pretrained token. If propose repeats the delegation idea in three sites, that's duplication begging to collapse — but the collapse target here is a *call* to grilling, not a re-description of its protocol.

- [x] Establish an isolated workspace for this unit off `port-wayfinder-siblings`: create a worktree on a dedicated branch (e.g. `unit-07-propose-critique-grilling`) and `cd` into it, confirming `git rev-parse --show-toplevel` ends in that worktree.

  **Workspace established — 0 images analyzed.** Created worktree at `/Users/caio.cavalcante/personal/hamilton/.worktrees/unit-07-propose-critique-grilling` on branch `unit-07-propose-critique-grilling`, based off `port-wayfinder-siblings` (`eb33381`). Confirmed `git rev-parse --show-toplevel` → `…/.worktrees/unit-07-propose-critique-grilling` and `branch --show-current` → `unit-07-propose-critique-grilling`. Subsequent unit-07 work runs from this worktree.

- [x] Run `hamilton-propose` for this unit, feeding it the unit 7 goal and ticket 12's resolved decisions as the change request (propose does not yet read routes — supply context explicitly). The proposal must encode ticket 12's exact boundary: grilling owns the **protocol only** (one question at a time, lead with a recommendation, look facts up rather than asking, never answer for the human); callers own question content and the exit condition. Answer any HITL questions from the ticket context so the phase is autonomous.

  **Propose complete — 0 images analyzed.** Ran `hamilton-propose` from the `unit-07-propose-critique-grilling` worktree, supplying the unit 7 goal and ticket 12's resolved decisions as the change request (propose does not yet read routes). Created four artifacts in `.hamilton/changes/2026-08-09-propose-critique-use-grilling/`:

  - `proposal.md` — encodes ticket 12's exact boundary: grilling owns the protocol only; callers own question content and the exit condition. Propose delegates at steps 4, 7, 10; critique gains grilling on the `changes-requested` path between the rubric and the report. Attendance guarded at each call site. "Judge, don't fix" preserved. No open questions — ticket 12 resolves all decisions. Status: approved (unattended, no open questions).
  - `requirements/propose.md` — 5 ADDED requirements: clarifying-questions delegation (step 4), approach-choice delegation (step 7), approval-loop delegation (step 10), attendance guard at each call site, no dialogue-protocol duplication remains. No canonical spec exists for `propose`; these are the first formal requirements.
  - `requirements/critique.md` — 5 ADDED requirements: grilling on changes-requested path, no grilling on approved path, report written from validated set, attendance guard, "Judge, don't fix" preserved. No canonical spec exists for `critique`; these are the first formal requirements.
  - `design.md` — 4 decisions: surgical extraction at each call site (vs shared section), step 7 retains approach-building, grilling positioned between rubric and report in critique, attendance guard at call site not in grilling. Quality Lens: all principles pass (SRP, DRY, low coupling, right-sized abstraction). No automated tests; verified by reading. Gates must stay green.

  Committed to `unit-07-propose-critique-grilling` branch and pushed to origin. Next step: `hamilton-plan`.

- [x] Run `hamilton-plan` on the approved propose artifacts to produce the ordered task ledger.

  **Plan complete — 0 images analyzed.** Ran `hamilton-plan` from the `unit-07-propose-critique-grilling` worktree (confirmed `git rev-parse --show-toplevel` → `…/.worktrees/unit-07-propose-critique-grilling`, branch `unit-07-propose-critique-grilling`). Read the approved propose artifacts (`proposal.md`, `design.md`, `requirements/propose.md`, `requirements/critique.md`), the canonical `dialogue.md` spec, both target skill files (`hamilton-propose/SKILL.md`, `hamilton-critique/SKILL.md`), `hamilton-grilling/SKILL.md`, the plan template, and the `code-quality.md` reference. Wrote `plan.md` with 4 TDD-sized tasks:

  - **Task 1:** Delegate propose's three dialogue surfaces (steps 4, 7, 10) to grilling — each with exact find-and-replace text, attendance guard, content + exit condition at call site.
  - **Task 2:** Remove protocol duplication from propose Principles section and process flow diagram (depends on Task 1).
  - **Task 3:** Add grilling to critique's `changes-requested` path between rubric and report — new step 6 with attendance guard, "Judge, don't fix" preserved, diagram updated (independent of Tasks 1–2).
  - **Task 4:** End-to-end sediment check on both skills + run gates (depends on Tasks 2, 3).

  Task seams follow the design's file boundary: propose edits are sequential (same file), critique is independent (different file), and the final cross-file sediment check operationalizes the route's craft focus ("what the deletions leave behind"). Every acceptance criterion cites a requirement scenario. No structural smells. Committed to `unit-07-propose-critique-grilling` branch and pushed to origin. Next step: dispatch the oracle agent to implement.

- [x] Dispatch the **oracle** agent to implement the refactor across both skills, giving it the propose + plan artifacts and the `writing-great-skills` reference. **Cap oracle retries at 2.** The edits must satisfy: propose delegates at steps 4, 7, and 10 (step 7 still builds the 2–3 approaches; grilling runs the ask); critique gains grilling on the `changes-requested` path only, between the rubric and the report, so `critique.md` is written from the validated set; **"Judge, don't fix" survives untouched** — critique still never edits `proposal.md`, `requirements/`, or `design.md`, and the revision loop stays with whoever runs the pipeline; attendance is guarded at each call site (attended → invoke grilling, unattended → fall back to the behaviour both skills already document); grilling itself never gains an unattended mode.

  **Implementation complete — 0 images analyzed.** Dispatched a specialist subagent with the plan (exact find-and-replace text), the propose artifacts, and the `writing-great-skills` reference. **Agent fallback:** the **oracle** agent's configured model (`ifood-messages/kimi-k3-tencent`) was unavailable — two dispatch attempts both failed with `Model not found`. Rather than block the playbook on a model outage, fell back to the **coder** agent (implementation specialist), the closest available subagent, preserving the route's delegation intent (separating planning from execution). The coder succeeded on the first attempt; the oracle retry cap (2) was not reached on implementation grounds — it was exhausted by the model outage. All 4 plan tasks executed in the `unit-07-propose-critique-grilling` worktree:

  - **Task 1** (`fca39ea`) — propose steps 4/7/10 delegate to `hamilton-grilling`. Step 4: clarifying questions as content, "intent is clear" exit, attendance guard. Step 7: **still builds** the 2–3 approaches + trade-offs + recommendation; only the ask delegates, "an approach is chosen" exit. Step 10: revision feedback as content, "artifacts approved" exit, "do not pass the gate until approved" retained.
  - **Task 2** (`db795b6`) — propose Principles "Collaborate." bullet trimmed to "confirm each section before moving on"; diagram node label `"Ask clarifying questions\n(one at a time)"` → `"Ask clarifying questions"` in the declaration + all 3 edges.
  - **Task 3** (`2bac56d`) — critique gains new step 6 (validate findings, `changes-requested` only) between verdict (5) and report (renumbered 7); exit "every finding is validated"; false positive rejectable; author picks among several fixes; unattended → name next step + return; `approved` path → grilling never runs; explicitly "validates not fixes" (propose artifacts untouched, revision loop external). Diagram rerouted: `changes-requested` edge through a new `"Validate findings with author\n(hamilton-grilling)"` node; `approved` edge bypasses it.
  - **Task 4** — end-to-end sediment check on both skills; **no sediment found**, so no commit (plan's file list is conditional: "only if sediment is found and fixed"; an empty commit would violate the no-empty-commits rule). Gates run as part of this task.

  Commits are **local** on `unit-07-propose-critique-grilling` (not pushed — `hamilton-finish-work` with `local-merge` handles the merge into `port-wayfinder-siblings`).

  **Independent verification (end-to-end, not just the diff):**
  - Every edit matches the plan's exact find-and-replace text verbatim — read both edited `SKILL.md` files in full.
  - `skills/hamilton-grilling/SKILL.md` diff = **0 lines** (untouched).
  - Protocol-language grep (`one at a time|multiple-choice|lead with your recommendation|get the requester|recommendation-led|never answer for`) → **zero hits** in both edited skills.
  - `hamilton-grilling` invoked at exactly the 4 call sites (propose 3, critique 1 + diagram references).
  - Grilling `unattended` grep → **zero hits** (never gained an unattended mode).
  - "Judge, don't fix" preserved: critique intro (lines 18–20) verbatim; step 6 states "validates findings — it does not fix them: the propose artifacts are not modified, and the revision loop stays with whoever runs the pipeline"; Output + Handoff sections consistent.
  - Gates: `bun run build` → exit 0; `bun --bun vitest run` → **24/24 tests pass** (3 files).

  Next step: `hamilton-review` on the unit's diff.

- [x] Run `hamilton-review` on the unit's diff. Beyond the standard checks, the review must verify the route's specific craft warning: read the edited steps **end-to-end, not just the diff** — no leftover half-instructions, no step that reads as a non-sequitur after the dialogue is extracted, no duplication between what propose still says about dialogue and what grilling now owns. Address any blocking findings.

  **Review complete — verdict: approved (0 blocking, 1 non-blocking suggestion) — 0 images analyzed.** Ran `hamilton-review` against the full unit diff (`git diff port-wayfinder-siblings...HEAD` on the `unit-07-propose-critique-grilling` worktree). Read `plan.md`, `design.md`, both requirements files, `proposal.md`, the `code-quality.md` rubric, both edited `SKILL.md` files end-to-end, and `hamilton-grilling/SKILL.md` (to fix the protocol boundary).

  - **All 10 requirement scenarios satisfied** (propose #1–#5, critique #1–#5). Step 7 still builds approaches; step 10 retains its gate; grilling runs on critique's `changes-requested` path only; "Judge, don't fix" preserved in intro, step 6, Output, and Handoff.
  - **Route craft warning verified (end-to-end, not just the diff):** read both edited skills start to finish. No leftover half-instructions, no non-sequiturs, no duplication. The only "recommendation" in step 7 is propose's domain content (which approach to recommend), not grilling's protocol. Protocol-language grep (`one at a time|multiple-choice|lead with your recommendation|get the requester|…`) → **zero hits** in both edited skills.
  - **Boundaries:** `hamilton-grilling/SKILL.md` diff is empty (unchanged); no unattended mode added to grilling (grep → zero hits). The `docs/skills.md` "skills do not call each other" sentence is now false but the design explicitly defers it to unit 9 (Non-Goal) — not a finding for this change.
  - **Gates re-run during review:** `bun run build` → exit 0; `bun --bun vitest run` → 24/24 pass (3 files).
  - **1 non-blocking suggestion:** critique step 6/7 leaves "chosen fix + alternatives recorded in `critique.md`" slightly implicit (the report-format `Fix:` field already supports sub-numbered options, so the behavior is achievable, just not stated in one place). Deferred to the mandated `writing-great-skills` pass (next task), the natural place to tighten it.
  - **No blocking findings to address** — verdict is `approved`; the pipeline proceeds to the `writing-great-skills` craft pass.

  **Write-boundary note.** The review's `review.md` + `progress.md` artifacts belong in the change directory (`.hamilton/changes/2026-08-09-propose-critique-use-grilling/`) on the `unit-07-propose-critique-grilling` branch, but that worktree is outside this agent's write boundary (`port-wayfinder-siblings`). Both are staged in the Auto Run working folder (`.maestro/playbooks/2026-08-08-Finish-Wayfinder-Route/Working/`). `hamilton-finish-work` runs from `port-wayfinder-siblings`; after it local-merges `unit-07-propose-critique-grilling`, the change dir will exist in `port-wayfinder-siblings` and the staged `review.md`/`progress.md` should be moved into it and committed as part of finish-work.

- [x] Run the `writing-great-skills` skill **explicitly** against both changed skills — `skills/hamilton-propose/` and `skills/hamilton-critique/` — and fix every finding in place. This is the mandated post-unit craft pass; pay particular attention to no-ops and duplication surfaced by the removal.

  **Craft pass complete — 0 images analyzed.** Ran `writing-great-skills` explicitly (read its `SKILL.md` + `GLOSSARY.md`) against both edited skills, read end-to-end per the route's craft warning ("what the deletions leave behind"). 4 findings, all fixed in place on the `unit-07-propose-critique-grilling` branch (commit `bb36725`, pushed):

  - **[Duplication + no-op, propose step 4]** Removed the trailing "Do not start drafting until the intent is clear." — it restates the exit condition ("intent is clear") the delegation made explicit, and is *inconsistent* with the unattended path (which records an assumption and proceeds, never reaching "clear"). Single source = the delegation's exit condition; unattended proxy = "record a reasonable choice as an assumption."
  - **[Duplication, critique step 6]** Removed "This step validates findings — it does not fix them: the propose artifacts are not modified, and the revision loop stays with whoever runs the pipeline." — the **third** copy of "Judge, don't fix" (intro 18–20, Output 162–165, and here). The intro is the authoritative single source; the Output restatement stays (distinct function: output contract). The revision-loop clause survives verbatim in the intro.
  - **[No-op, critique step 6]** Removed "On the `approved` path there are no findings, so grilling never runs." — entailed by the step title "(changes-requested only)" + the first sentence's conditional; the agent won't invoke grilling on approved because the condition excludes it. The title is the single source for branch scope.
  - **[Co-location, critique report format]** `Fix:` field now reads "sub-number 1./2. when there are options, marking the author's choice" — the validated-fix recording now lives in one place. This closes the review's deferred non-blocking note (step 6/7 left the chosen-fix recording implicit).

  Considered and **kept**: propose step 10's "Do not pass the gate until approved" (correct for both attended and unattended paths; safety closure the Handoff explicitly references as "the step-10 gate"); critique intro + Output "don't fix" restatement (pre-existing, distinct functions — principle vs. output contract). Zero protocol-language duplication remains: grep for `one at a time|multiple-choice|lead with your recommendation|get the requester|recommendation-led|never answer for` → **zero hits** in both edited skills. `hamilton-grilling/SKILL.md` diff = **0 lines** (untouched). Grilling invoked at exactly 4 call sites (propose 3, critique 1) + diagram references. Gates: `bun run build` → exit 0; `bun --bun vitest run` → **24/24 pass** (3 files). Next step: `hamilton-finish-work` with `local-merge` into `port-wayfinder-siblings`.

- [ ] Run `hamilton-finish-work` with the **`local-merge`** strategy, merging the unit's branch back into **`port-wayfinder-siblings`** (not `main`), from the `port-wayfinder-siblings` worktree. Tear down the unit worktree after the merge; confirm the spec sync and `progress.md` finish entry ride into `port-wayfinder-siblings`.

- [ ] Verify the unit shipped: in `.hamilton/maps/hamilton-wayfinder/route.md` the `### 7.` section's `Status:` line now reads `shipped`. Run the repo gates — `bun run build` and `bun --bun vitest run` — and confirm both pass. No test asserts on skill content (per ticket 12), but the gates must stay green.
