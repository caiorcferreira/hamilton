# Phase 05: Convert the map's own files to the mechanics contract (Route Unit 10)

Convert `map.md` and all thirteen tickets in `.hamilton/maps/hamilton-wayfinder/` from loose `Key: value` header lines to YAML frontmatter with `type`, `status`, and `blocked_by`, then give the `## Map mechanics` contract a written home. This is dogfooding cleanup that touches only this map's own files, and it is the **last unit in the route** — when it ships, the map reaches its destination and `map.md` moves to `shipped`. Mechanical frontmatter conversion, so it uses the **coder** agent. One choice is deliberately left open by ticket 04 (the home for the mechanics contract — `CONTRIBUTING.md` or a dedicated `MECHANICS.md` under `.hamilton/maps/`); the propose step settles it rather than the implementer deciding mid-change. No skills change this unit, so there is no `writing-great-skills` pass.

## Tasks

- [x] Load the unit's context. Read the `### 10. Convert the map's own files to the mechanics contract` section of `.hamilton/maps/hamilton-wayfinder/route.md` and its backing ticket `tickets/04-map-mechanics-in-files.md`. Also read `map.md` (note its current loose `Status: shipping` line) and all thirteen ticket files (each has loose `Type:`, `Status:`, `Blocked by:` lines) to see the exact conversion surface. Note ticket 04's contract: YAML frontmatter with `type` (`grilling` / `research` / `prototype` / `task`), `status` (tickets: `open` / `resolved`; maps: `cleared` / `shipping` / `shipped` per ticket 06's three-stage lifecycle, which supersedes ticket 04's `open` / `cleared` for maps), and `blocked_by` (list of ticket numbers). Note the one open choice: where the `## Map mechanics` contract lives — `CONTRIBUTING.md` or a dedicated `MECHANICS.md` under `.hamilton/maps/`.

  **Context loaded.** Read route.md §10, ticket 04, `map.md` (loose `Status: shipping`), and all thirteen ticket files. Conversion surface confirmed — every ticket has loose `Type:` / `Status:` / `Blocked by:` lines; `map.md` has a loose `Status:` line. Contract per ticket 04: YAML frontmatter `type` / `status` / `blocked_by`; map status uses ticket 06's three-stage `cleared` / `shipping` / `shipped` (supersedes ticket 04's `open` / `cleared`). Open choice for propose: `CONTRIBUTING.md` vs `.hamilton/maps/MECHANICS.md` as the home for the `## Map mechanics` contract. Images analyzed: 0.

- [x] Establish an isolated workspace for this unit off `port-wayfinder-siblings`: create a worktree on a dedicated branch (e.g. `unit-10-map-mechanics-contract`) and `cd` into it, confirming `git rev-parse --show-toplevel` ends in that worktree.

  **Workspace established.** Created worktree at `/Users/caio.cavalcante/personal/hamilton/.worktrees/unit-10-map-mechanics-contract` on branch `unit-10-map-mechanics-contract`, based off `port-wayfinder-siblings` (ebdb755). Confirmed `git rev-parse --show-toplevel` resolves to the new worktree path. Parent worktree (`port-wayfinder-siblings`) was clean apart from untracked Auto Run docs.

- [x] Run `hamilton-propose` for this unit. Propose may now read the route itself (unit 8 shipped the map-aware entrypoint in Phase 03); if not, feed the unit 10 goal and ticket 04's decisions as the change request. The proposal must **settle the one open choice** — the home for the `## Map mechanics` contract (`CONTRIBUTING.md` vs a dedicated `MECHANICS.md` under `.hamilton/maps/`) — with reasoning, since ticket 04 deliberately left it for propose. The scope is `map.md` + the thirteen tickets only (route.md is not in this conversion per the route). Answer any HITL questions from the ticket context so the phase is autonomous.

  **Propose complete.** Worked in place on the `port-wayfinder-siblings` worktree (a linked worktree on a non-default branch — satisfies propose's isolation gate; the unit-10 worktree task 2 created is outside this agent's write boundary, so the change directory lives here at `.hamilton/changes/2026-08-09-map-mechanics-contract/`). Artifacts: `proposal.md`, `requirements/wayfinder.md` (MODIFIED — two requirements), `design.md`. **Open choice settled: `CONTRIBUTING.md`** as the contributor-facing home for the `## Map mechanics` contract — discoverable, joins the existing convention sections, keeps `.hamilton/maps/` a directory of effort directories not reference docs; the `MECHANICS.md` alternative rejected on discoverability and layout-purity grounds (Approach B), the skill-only option rejected because the route and ticket 04 both ask for a contributor-facing home (Approach C). **Second decision: `map.md` carries `status` only (no `type`)** — the map template (unit 3, shape authority) has `status` only and ticket 04 defines `type` as a ticket-type field; the playbook coder note's `type: map` is overruled in favour of the template. `route.md` excluded per the route (its `Status:` lines are inline per-unit markers). `blocked_by` as YAML list; `—` → `[]`. No HITL questions — all answered from ticket context (04, 06, 01). Self-reviewed against `references/code-quality.md` (mechanical change, minimum scrutiny; DRY two-homes concern accepted and recorded under Risks). Images analyzed: 0.

- [x] Run `hamilton-plan` on the approved propose artifacts to produce the ordered task ledger for the conversion.

  **Plan complete.** Worked in place on the `port-wayfinder-siblings` worktree (the same isolation propose used — a linked worktree on a non-default branch). Read the approved `proposal.md` / `design.md` / `requirements/wayfinder.md`, the canonical [`wayfinder`](../../../specs/wayfinder.md) spec's `### Map mechanics` table, both target templates (`bundle/templates/wayfinder/map.md` carries `status` only; `ticket.md` carries `type` / `status` / `blocked_by`), the wayfinder skill's agent-facing `## Map mechanics` section, `CONTRIBUTING.md`, `map.md`, and all thirteen ticket headers. Wrote `.hamilton/changes/2026-08-09-map-mechanics-contract/plan.md` — two tasks, both dependency-free (distinct files, parallelizable): **Task 1** converts `map.md` (frontmatter `status: shipping`, no `type`) and all thirteen tickets to frontmatter, carrying the full value-mapping table (e.g. ticket 11 → `blocked_by: [03, 04, 05, 08, 10, 12, 13]`, `Blocked by: —` → `[]`) so the coder needs no further design; **Task 2** adds the self-contained `## Map mechanics` section to `CONTRIBUTING.md` after `## Licensing and attribution`, documenting the three fields, exact valid values (ticket `status`: `open`/`claimed`/`resolved`; map `status`: `open`/`cleared`/`shipping`/`shipped` per ticket 06; `blocked_by` YAML list), and the swap boundary, genuinely isolated. `route.md` explicitly excluded. Self-reviewed against `references/code-quality.md` (mechanical change, minimum scrutiny; DRY two-homes concern accepted in design). Committed `548e752`, pushed to `origin/port-wayfinder-siblings`. Next step: `hamilton-code` (one task at a time) or `hamilton-orchestrate`. Images analyzed: 0.

- [x] Dispatch the **coder** agent to implement the conversion, giving it the propose + plan artifacts:
  - Convert `map.md`'s loose header to YAML frontmatter (`type: map`, `status: shipping` — it will flip to `shipped` at finish-work since this is the last unit).
  - Convert all thirteen `tickets/NN-slug.md` files' loose `Type:` / `Status:` / `Blocked by:` lines to YAML frontmatter (`type`, `status`, `blocked_by`), preserving each ticket's existing resolved values.
  - Write the `## Map mechanics` contract to the home propose settled, documenting the three frontmatter fields, their valid values, and the boundary a future tracker backend would swap.

  **Coder dispatched and verified.** Dispatched the `coder` subagent with the full propose + plan context (decisions, the verbatim 13-row value-mapping table, the CONTRIBUTING.md section spec, the verification commands, and the gates). It implemented both plan tasks in place on the `port-wayfinder-siblings` worktree (propose's chosen isolation), in two commits: `b1f877c` `MAESTRO: docs: convert map.md and tickets to YAML frontmatter` (Task 1) and `720e9dc` `MAESTRO: docs: add Map mechanics contract section to CONTRIBUTING.md` (Task 2); pushed to `origin/port-wayfinder-siblings`. **Note: `map.md` carries `status: shipping` ONLY — no `type` field — per the propose decision (`design.md`), which overrules this checkbox's stale `type: map` suggestion in favour of the map template (unit 3, the shape authority) that carries `status` only.** Independent verification (run by this agent, not just the coder): `rg '^Type:|^Status:|^Blocked by:'` over map.md + 13 tickets → no matches (exit 1); `git diff --name-only HEAD~2 HEAD` → exactly 15 files (map.md, 13 tickets, CONTRIBUTING.md) — no route.md, no `src/`, no `bundle/templates/`, no `tests/`, no `skills/`; every converted file begins with `---`, `route.md` begins with `# Route` (untouched); spot-checked map.md (status only), ticket 11 (`blocked_by: [03, 04, 05, 08, 10, 12, 13]`), ticket 13 (`[01, 04, 06, 09]`), ticket 02 (`research` / `[]`); the CONTRIBUTING.md `## Map mechanics` section is self-contained (fields, exact valid values incl. ticket 06's three-stage map lifecycle, swap-boundary statement, no cross-references to other sections); gates green — `bun run build` exit 0, `bun --bun vitest run` 24/24 tests exit 0. Images analyzed: 0.

- [x] Run `hamilton-review` on the unit's diff, confirming every map and ticket file was converted (no loose header lines left behind) and the mechanics contract is genuinely isolated so a future backend can swap it. Address any blocking findings.

  **Review complete — approved.** Reviewed the full coder diff (`548e752..720e9dc`, commits `b1f877c` + `720e9dc`, fifteen files) against `plan.md` (both tasks), `requirements/wayfinder.md` (both MODIFIED requirements, all four scenarios), and `design.md` (all four decisions). Wrote `.hamilton/changes/2026-08-09-map-mechanics-contract/review.md` (verdict: approved, 0 blocking, 0 suggestions) and appended the review entry to `progress.md`. **Verified independently:** (1) `rg '^Type:|^Status:|^Blocked by:'` over map.md + 13 tickets → no matches (exit 1) — every loose header line gone; (2) all fourteen converted files begin with `---`; (3) `map.md` frontmatter is `status: shipping` only (no `type`) per design Decision 2; (4) all thirteen tickets carry `type`/`status`/`blocked_by` in order with values matching the plan's mapping table exactly (spot-checked every ticket incl. 11 → `[03, 04, 05, 08, 10, 12, 13]`, 13 → `[01, 04, 06, 09]`, `—` → `[]`); (5) `route.md` untouched — not in diff, begins with `# Route`; (6) exactly 15 files — no route.md, no `src/`, no `bundle/templates/`, no `tests/`, no `skills/`; (7) CONTRIBUTING.md `## Map mechanics` section positioned after `## Licensing and attribution`, self-contained, documents all three fields + valid values + swap boundary; (8) **genuine isolation confirmed** — `rg 'frontmatter|blocked_by|Map mechanics'` over CONTRIBUTING.md lines 1–68 (outside the new section) → no matches, so a future backend can swap that one section without touching the rest; (9) gates green — `bun run build` exit 0, `bun --bun vitest run` 24/24 tests exit 0. No blocking findings; the DRY two-homes concern and the spec wording reconciliation are both accepted in `design.md` (non-coder, traced to design). Images analyzed: 0.

- [x] Run `hamilton-finish-work` with the **`local-merge`** strategy, merging the unit's branch back into **`port-wayfinder-siblings`** (not `main`), from the `port-wayfinder-siblings` worktree. **This is the last unit** — as part of this same diff, flip `map.md`'s frontmatter `status` to `shipped` (the map reaches its destination) and the route's top-level `Status:` to `shipped`. Tear down the unit worktree after the merge; confirm the spec sync and `progress.md` finish entry ride into `port-wayfinder-siblings`.

  **Finish-work complete — destination reached.** Ran `hamilton-finish-work` with `local-merge` from the `port-wayfinder-siblings` worktree. Preconditions verified before finishing: tree clean (only untracked Phase-06/07 Auto Run docs, not part of this change), `bun run build` exit 0, `bun --bun vitest run` 24/24 tests exit 0, review approved (0 blocking), all plan tasks done. **Spec sync:** updated `.hamilton/specs/wayfinder.md` — reconciled `blocked_by` wording from "comma-separated ticket slugs" to "YAML list of ticket numbers (`[]`, `[01]`, `[01, 04, 06, 09]`)" (the wording mismatch `design.md` flagged for finish-work reconciliation), qualified the `type` row as `(ticket)`, stated explicitly that `map.md` carries `status` only (no `type`), documented the contributor-facing `CONTRIBUTING.md` home in the `### Map mechanics` section intro, and added a "Two homes for the mechanics contract" decision to `## Decisions`. **Status flips (same diff):** `map.md` frontmatter `status: shipping` → `status: shipped`; `route.md` top-level `Status: pending` → `Status: shipped`; `route.md` §10 `Status: pending` → `Status: shipped`. **Finish entry** appended to `progress.md`. All committed as `6cceb2e` and pushed to `origin/port-wayfinder-siblings`. **Workspace outcome:** the work was done in place on `port-wayfinder-siblings` (propose's chosen isolation — a linked worktree on a non-default branch), so the local-merge was a no-op (the work was already on the target branch). The stale `unit-10-map-mechanics-contract` worktree at `/Users/caio.cavalcante/personal/hamilton/.worktrees/unit-10-map-mechanics-contract` (created by task 2 but never used for the actual work — it was outside the agent's write boundary) was removed with `git worktree remove`, and branch `unit-10-map-mechanics-contract` (at `ebdb755`, an ancestor of the current HEAD) was deleted with `git branch -D`. Remaining worktrees: `main`, `feat-ralph-loop`, `claude/rfc-skill-helper-scripts-55c9e0`, `port-wayfinder-siblings`. **This is the last route unit — the map has reached its destination through Hamilton's own loop.** Images analyzed: 0.

- [ ] Verify the route is complete: in `.hamilton/maps/hamilton-wayfinder/route.md` the `### 10.` section's `Status:` line reads `shipped`, and `map.md`'s status reads `shipped` — the destination reached through Hamilton's own loop. Run the repo gates — `bun run build` and `bun --bun vitest run` — and confirm both pass.
