# Phase 01: Author hamilton-wayfinder (Route Unit 6)

Author `skills/hamilton-wayfinder/` — the centerpiece skill written from scratch, with its own `SKILL.md`, a `references/` folder where the design calls for it, and a sibling `NOTICE`. This is the longest, most-invoked skill in the repo and the one unit where nothing upstream constrains the shape, so it carries the most craft weight. It runs the full SDD loop (`propose → plan → code → review → finish-work`) and lands as a complete, invokable skill — the working prototype this route produces. All eight backing decisions are resolved in the map's tickets; the proposing agent answers any HITL questions from that ticket context so the phase is fully autonomous. Uses the **oracle** agent for the skill-authoring step (expensive model — retries capped at 2).

## Tasks

- [x] Load the unit's context before anything else. Read the `### 6. Author hamilton-wayfinder` section of `.hamilton/maps/hamilton-wayfinder/route.md` and all eight backing tickets it links: `tickets/01-map-artifact-layout.md`, `04-map-mechanics-in-files.md`, `05-template-convention.md`, `06-route-shape-and-sdd-join.md`, `07-which-siblings-to-port.md`, `08-ticket-types.md`, `09-boundary-with-propose-and-critique.md`, `13-map-artifacts-and-worktrees.md`. Also read the route's preamble (the rules on verbatim ports, the `writing-great-skills` mandate, and status-flip mechanics) and the one-line decisions in `map.md`'s **Decisions so far**.

  **Context loaded.** Read route.md section 6 (centerpiece: author `skills/hamilton-wayfinder/SKILL.md` + `references/` + `NOTICE` from scratch), its preamble (verbatim-port rule, writing-great-skills mandate for units 4-7, status-flips ride the unit's own branch per ticket 13), and map.md's Decisions so far (13 resolved tickets, map cleared). All eight backing tickets digested:
  - **01**: `.hamilton/maps/<effort>/` as third sibling to specs/ and changes/; undated slug; `tickets/NN-slug.md`; no asset dir; hamilton-init does NOT scaffold maps/; cleared map stays in place.
  - **04**: YAML frontmatter (`type`/`status`/`blocked_by`); claiming stays (signals intent); status: open/resolved (tickets), open/cleared (maps); one `## Map mechanics` section as the swappable contract.
  - **05**: All three shapes (`map.md`/`ticket.md`/`route.md`) go in `bundle/templates/wayfinder/`; wayfinder depends on `hamilton setup` having been run.
  - **06**: route.md = static handoff, written once at map close; each unit has name/goal/decision-links/deps/entry; route points, doesn't restate; status per unit: pending/in-progress/shipped; map: cleared→shipping→shipped.
  - **07**: All three siblings ported verbatim under `hamilton-wayfinder-*` prefix, internal to wayfinder only; grilling ports separately as `hamilton-grilling`; full ports not trimmed.
  - **08**: All four ticket types survive (research AFK, prototype HITL, grilling HITL, task HITL/AFK); type in YAML frontmatter; wayfinder keeps strict HITL rule for planning; Hamilton's three-tier model applies to SDD execution not planning.
  - **09**: One change = one session; every unit goes through propose (no straight-to-plan); propose gains map-aware entrypoint (reads route.md, finds next pending unit, pulls ticket context); no critique equivalent for maps.
  - **13**: Status flip rides the unit's own branch, lands on default branch at merge; `.hamilton/maps/` is ordinary repo content; between-merge staleness accepted; SKILL.md states this rule in `## Map mechanics`.

- [ ] Read `/writing-great-skills` (SKILL.md at `~/.agents/skills/writing-great-skills/SKILL.md`) **before drafting** — it is `disable-model-invocation: true`, so it must be read explicitly and no agent reaches for it alone. Study: invocation-mode trade-offs, the body-vs-`references/` information hierarchy, the `## Map mechanics` isolation boundary, and the leading-words / pruning / no-op guidance. Then read the three already-ported siblings (`skills/hamilton-wayfinder-research/`, `skills/hamilton-wayfinder-prototype/`, `skills/hamilton-wayfinder-domain-modeling/`) and `skills/hamilton-grilling/` to absorb the established adaptation surface — the provenance line, the `NOTICE` file shape (see `skills/hamilton-grilling/NOTICE`), and how `references/` is used.

- [ ] Establish an isolated workspace for this unit off `port-wayfinder-siblings`: create a worktree on a dedicated branch (e.g. `git worktree add .worktrees/unit-06-author-hamilton-wayfinder -b unit-06-author-hamilton-wayfinder` off `port-wayfinder-siblings`), then `cd` into it and confirm `git rev-parse --show-toplevel` ends in that worktree. This keeps the unit's work off `port-wayfinder-siblings` until finish-work merges it back.

- [ ] Run `hamilton-propose` for this unit, feeding it the unit 6 goal and backing decisions from the route + tickets as the change request (propose does not yet read routes automatically — that capability lands in unit 8 — so supply the context explicitly). Answer every clarifying question from the ticket context, since the decisions are already resolved and propose's job is to shape the skill, not re-litigate them. Three open choices the proposal must settle, each costed either way: **body versus `references/`** for explanatory material; the **`## Map mechanics`** isolation boundary (pluggability is only real if the section is genuinely isolated); and **invocation mode** (note its interaction with unit 5 — if this skill must reach the ported siblings, they cannot be user-invoked).

- [ ] Run `hamilton-plan` on the approved propose artifacts to produce an ordered, TDD-sized task ledger in `plan.md` for the skill authoring.

- [ ] Dispatch the **oracle** agent to implement the skill: `skills/hamilton-wayfinder/SKILL.md`, whatever `references/` files the design calls for, and a sibling `NOTICE`. Give the oracle the propose + plan artifacts, the `writing-great-skills` reference, and the route's craft-focus paragraph. **Cap oracle retries at 2** to control cost — if it cannot converge, stop and report rather than retry indefinitely. Hard requirements from the tickets: file-native throughout (maps at `.hamilton/maps/<effort>/` holding `map.md`, `route.md`, `tickets/NN-slug.md`); tracker mechanics isolated in **one** `## Map mechanics` section; map artifacts stated as ordinary repo content versioned and branched like source (ticket 13); all four ticket types surviving with the strict HITL rule for planning (ticket 08); the provenance line and `NOTICE` matching the established sibling pattern.

- [ ] Run `hamilton-review` on the unit's diff, judging it against the plan and the route's craft focus ("the bar is the craft itself, not just faithful transcription of thirteen decisions"). Address any blocking findings the review raises before proceeding.

- [ ] Run the `writing-great-skills` skill **explicitly** against `skills/hamilton-wayfinder/` (read its SKILL.md and apply every lever: invocation mode, description pruning, single-source-of-truth, no-ops, leading words, sediment). Fix every finding it surfaces in place — this is the mandated post-unit craft pass for the skill this phase changed.

- [ ] Run `hamilton-finish-work` with the **`local-merge`** strategy, merging the unit's branch (`unit-06-author-hamilton-wayfinder`) back into **`port-wayfinder-siblings`** — not `main`. Finish-work runs the merge from the `port-wayfinder-siblings` worktree; tear down the unit worktree after the merge. Confirm the spec sync (any capability spec created/updated) and the `progress.md` finish entry are committed before the merge so they ride into `port-wayfinder-siblings`.

- [ ] Verify the unit shipped: in `.hamilton/maps/hamilton-wayfinder/route.md` the `### 6.` section's `Status:` line now reads `shipped` (this flip rides the unit's own branch and landed with the merge, per ticket 13). Then run the repo gates — `bun run build` and `bun --bun vitest run` — and confirm both pass. No test asserts on skill content (per ticket 12), but the gates must stay green.
