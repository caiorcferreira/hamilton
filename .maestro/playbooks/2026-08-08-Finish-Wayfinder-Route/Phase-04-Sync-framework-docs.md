# Phase 04: Sync the framework docs (Route Unit 9)

Add the `hamilton-wayfinder` entry to `docs/skills.md` in the established format (positioned before `hamilton-propose` as an optional pre-change planning stage), and add the `New/changed map artifacts in .hamilton/maps/` → `docs/skills.md` row to `CONTRIBUTING.md`'s mapping table. The `docs/skills.md` entry also carries the fork's provenance in prose — the one place a reader learns where wayfinder came from, with a link to `NOTICE` for the legal credit. Docs-only, mechanical, so it uses the **coder** agent. **Hold the scope literally:** ticket 10 restricts edits to `docs/skills.md` and `CONTRIBUTING.md` only — the six-skill diagram in `README.md` and `docs/sdd-framework.md` stays exactly as it is. That is the decision, not an oversight; resist widening it while editing. No skills change this unit, so there is no `writing-great-skills` pass.

## Tasks

- [x] Load the unit's context. Read the `### 9. Sync the framework docs` section of `.hamilton/maps/hamilton-wayfinder/route.md` and its two backing tickets: `tickets/10-framework-docs-presentation.md` (the docs-shape decision — wayfinder sits before propose as optional; pipeline phrased as "six core skills in fixed sequence, plus an optional pre-change planning stage"; fork provenance narrative lives in the `docs/skills.md` entry, brief, linking to `NOTICE`; scope held to `docs/skills.md` + `CONTRIBUTING.md` only) and `tickets/03-fork-attribution.md` (provenance is prose here, legal credit stays in `NOTICE`). Also read the current `docs/skills.md` to learn the established entry format (`### \`hamilton-...\`` headings with When / What / Source structure) and `CONTRIBUTING.md`'s mapping table.

  **Context loaded — 4 sources read (route.md §9, tickets 10 + 03, `docs/skills.md`, `CONTRIBUTING.md` mapping table); `NOTICE`/`LICENSE` + `skills/hamilton-wayfinder/{SKILL.md,NOTICE}` verified present. Load-bearing constraints for the downstream propose/plan/code tasks:**

  - **Scope (ticket 10):** edit `docs/skills.md` + `CONTRIBUTING.md` ONLY. No touches to `README.md` or `docs/sdd-framework.md` — the six-skill diagram stays exactly as it is. This is the decision, not an oversight; resist widening while editing.
  - **Position (ticket 10):** the `hamilton-wayfinder` entry goes **before** `hamilton-propose`, as an optional pre-change planning stage.
  - **Pipeline phrasing (ticket 10):** "Six core skills in fixed sequence, plus an optional pre-change planning stage (wayfinder)."
  - **Provenance (tickets 03 + 10):** carry the fork's provenance in **prose** inside the `docs/skills.md` wayfinder entry — brief note that it is a fork of upstream `mattpocock/skills` (MIT), with a link to `NOTICE` for full legal credit. Legal credit stays in `NOTICE`; `docs/sdd-framework.md`'s Inspirations section stays untouched.
  - **CONTRIBUTING.md row (ticket 10):** add `New/changed map artifacts in .hamilton/maps/` → `docs/skills.md`. A **different** row already exists for wayfinder artifact *templates* in `bundle/templates/wayfinder/` (line 15) — add the new map-artifacts row, do not duplicate or conflate the two.
  - **Established entry format (`docs/skills.md`):** `### \`hamilton-...\`` heading with a short role + step tag, a 1–2 sentence intro, then `- **When:**` / `- **Inputs:**` / `- **Produces:**` / `- **Notes:**` bullets, ending with `- Source: [\`skills/...\`](...)`.
  - **One-sentence rule for the entry** (carried verbatim in the implementation task below, attributed to ticket 09): "Use wayfinder to break a complex goal into clear, realizable units. Use hamilton-propose to transform each route unit into a concrete change spec ready for autonomous implementation."

- [x] Establish an isolated workspace for this unit off `port-wayfinder-siblings`: create a worktree on a dedicated branch (e.g. `unit-09-sync-framework-docs`) and `cd` into it, confirming `git rev-parse --show-toplevel` ends in that worktree.

  **Workspace established — 0 images analyzed.** Created worktree at `/Users/caio.cavalcante/personal/hamilton/.worktrees/port-wayfinder-siblings/.worktrees/unit-09-sync-framework-docs` on branch `unit-09-sync-framework-docs`, based off `port-wayfinder-siblings` (`d57c01f`, the tip that includes Phase-04 task 1's context load). Confirmed `git rev-parse --show-toplevel` → `…/.worktrees/unit-09-sync-framework-docs`, `branch --show-current` → `unit-09-sync-framework-docs`, base commit `d57c01f`, and `git rev-parse --git-dir` ≠ `--git-common-dir` (linked worktree).

  **Path note — nested inside the write boundary, following the unit-8 precedent.** Unit-7's sibling worktree sat outside this agent's hard write boundary and forced the Auto Run `Working/`-folder staging dance; unit-8 fixed that by nesting under `port-wayfinder-siblings/.worktrees/`. Unit-09 follows the same nesting, keeping the worktree inside the `port-wayfinder-siblings/` write boundary so every subsequent task can write change artifacts directly to the unit branch — no staging, no boundary workarounds. Ticket 13's rule is fully satisfied (unit rides its own branch; the status flip rides that branch; propose reads `route.md` from the branch the session started on) — only the on-disk path of the worktree differs, which ticket 13 does not constrain. `.worktrees/` is gitignored, so the nested worktree does not pollute `port-wayfinder-siblings`'s `git status` (verified: parent status shows only the pre-existing untracked Phase-05–07 playbook docs, not the worktree).

  Subsequent unit-09 work runs from this worktree: `cd .worktrees/unit-09-sync-framework-docs` from the `port-wayfinder-siblings` worktree root. Branch `unit-09-sync-framework-docs` is local for now; it pushes with the first artifact commit, matching the unit-8 cadence.

- [x] Run `hamilton-propose` for this unit. Propose may now read the route itself (unit 8 shipped the map-aware entrypoint in Phase 03); if not, feed the unit 9 goal and ticket 10 / 03 decisions as the change request. The proposal must hold the scope literally — `docs/skills.md` and `CONTRIBUTING.md` only, no edits to `README.md` or `docs/sdd-framework.md`. Answer any HITL questions from the ticket context so the phase is autonomous.

  **Propose complete — 0 images analyzed.** Ran `hamilton-propose` in the unit-09 worktree (`.worktrees/unit-09-sync-framework-docs`, branch `unit-09-sync-framework-docs`), feeding the unit-9 goal and the ticket 10 / 03 / 09 decisions as the change request. The map-aware entrypoint (unit 8) was not exercised — the unit-09 worktree was already created by task 2, so propose ran in place against the loaded context rather than re-reading the route. Produced three artifacts in `.hamilton/changes/2026-08-09-sync-framework-docs/`:

  - **`proposal.md`** — why: wayfinder shipped in unit 6 but is invisible in `docs/skills.md`, and provenance is invisible in the narrative docs after ticket 03 ruled it out of `docs/sdd-framework.md`'s Inspirations. Goals: the six success criteria (entry before propose, one-sentence rule, provenance prose + NOTICE link, pipeline phrasing, CONTRIBUTING row, gates green). Non-Goals hold the literal scope: no `README.md`, no `docs/sdd-framework.md`, no diagram redraw, no code/tests, no canonical spec. Capabilities: MODIFIED `framework-docs` (first-time tracking, matching unit-8 precedent). No open questions — tickets 03, 09, 10 decide everything.
  - **`requirements/framework-docs.md`** — six ADDED requirements with WHEN/THEN scenarios: (1) wayfinder entry in the skills reference, (2) one-sentence rule stated, (3) fork provenance in prose with NOTICE link, (4) pipeline identity phrasing adjusted, (5) CONTRIBUTING.md mapping row for map artifacts, (6) scope held literally to the two files. MODIFIED/REMOVED/RENAMED: none (no canonical spec exists).
  - **`design.md`** — Approach A (follow ticket 10 literally) chosen over B (widen to README/sdd-framework — rejected, re-litigates ticket 10/03) and C (defer CONTRIBUTING row — rejected, ticket 10 bundles them). Five decisions: entry before propose / after init; provenance in Notes bullet (not a subsection); pipeline phrasing replaces the identity sentence, not the diagram; CONTRIBUTING row adjacent to the existing templates row (distinct, not merged); no tests, no canonical spec. Architecture fixes the three `docs/skills.md` landing points and the one `CONTRIBUTING.md` row. Testing strategy is read-and-inspect (no automated tests on `docs/`). Quality Lens: mechanical docs-only change, DRY holds, no gate failure.

  Self-review passed: workspace confirmed (`git rev-parse --show-toplevel` ends in `.worktrees/unit-09-sync-framework-docs`; `--git-dir` ≠ `--git-common-dir`), all artifacts under the worktree root, no placeholders or template hints, scope held literally, design cleared the `code-quality.md` rubric at minimum scrutiny. Committed as `4a07e64` and pushed `unit-09-sync-framework-docs` to origin. **Next step is `hamilton-plan`** (the following task) — not invoked here, per the propose handoff.

- [x] Run `hamilton-plan` on the approved propose artifacts to produce the ordered task ledger for the two doc edits.

  **Plan complete — 0 images analyzed.** Ran `hamilton-plan` in the unit-09 worktree (`.worktrees/unit-09-sync-framework-docs`, branch `unit-09-sync-framework-docs`), reading the approved propose artifacts (`proposal.md`, `requirements/framework-docs.md`, `design.md`), the target files (`docs/skills.md`, `CONTRIBUTING.md`), the wayfinder `SKILL.md`, the root `NOTICE`, route.md §9, and `references/code-quality.md`. Produced `.hamilton/changes/2026-08-09-sync-framework-docs/plan.md` — an ordered ledger of **three TDD-sized tasks** cut along the design's three landing points, each mapping to a distinct requirement and independently verifiable by read-and-inspect (no automated test asserts on `docs/`):

  - **Task 1 — add the `hamilton-wayfinder` entry to `docs/skills.md`**: positioned between `hamilton-init` and `hamilton-propose`; established When/Inputs/Produces/Notes/Source format; carries the one-sentence rule (ticket 09) verbatim and the fork provenance in prose (fork of `mattpocock/skills`, MIT, link to `NOTICE`) in the Notes bullet. (Reqs 1, 2, 3.)
  - **Task 2 — adjust the pipeline identity phrasing in `docs/skills.md`**: replace "Seven skills." with "Six core skills in fixed sequence, plus an optional pre-change planning stage (wayfinder)." Depends on Task 1 (same file). (Req 4.)
  - **Task 3 — add the map-artifacts mapping row to `CONTRIBUTING.md`**: `New/changed map artifacts in .hamilton/maps/` → `docs/skills.md`, adjacent to and distinct from the existing `bundle/templates/wayfinder/` templates row. Independent (different file — may run in parallel with Task 1). (Req 5.)

  The literal-scope guard (Req 6: only `docs/skills.md` + `CONTRIBUTING.md`; no `README.md`, no `docs/sdd-framework.md`) is a "Done when" criterion checked via `git diff --name-only`. Self-review passed: worktree confirmed (toplevel ends in `.worktrees/unit-09-sync-framework-docs`; `--git-dir` ≠ `--git-common-dir`), every task independently verifiable with a concrete `grep` verify command, acceptance criteria cite the requirement scenarios, dependencies acyclic (Task 2 → Task 1; Task 3 free), no code snippets to review (docs-only), DRY holds. Baseline gates green before commit: `bun run build` exit 0, `bun --bun vitest run` 24/24 passed. Committed as `3bf05e7` and pushed `unit-09-sync-framework-docs` to origin. **Next step is the coder dispatch** (the following task) — `plan.md` is the handoff contract; `hamilton-code` consumes one task at a time.

- [x] Dispatch the **coder** agent to implement both edits, giving it the propose + plan artifacts:
  - **`docs/skills.md`** — add a `### \`hamilton-wayfinder\`` entry in the established format, positioned **before** `hamilton-propose` as an optional pre-change planning stage. State the one-sentence rule from ticket 09 ("Use wayfinder to break a complex goal into clear, realizable units. Use hamilton-propose to transform each route unit into a concrete change spec ready for autonomous implementation."). Carry the fork's provenance in prose — brief note that it is a fork of upstream work, with a link to `NOTICE` for full legal credit. Adjust the pipeline identity phrasing to "six core skills in fixed sequence, plus an optional pre-change planning stage (wayfinder)."
  - **`CONTRIBUTING.md`** — add the mapping-table row `New/changed map artifacts in .hamilton/maps/` → `docs/skills.md`. Note: a **different** row already exists for wayfinder artifact *templates* in `bundle/templates/wayfinder/` (added by unit 3) — add the new map-artifacts row, do not duplicate or conflate the two.

  **Coder dispatched — 0 images analyzed.** Dispatched a coder subagent (Task tool, `coder` type) against the unit-09 worktree (`.worktrees/unit-09-sync-framework-docs`, branch `unit-09-sync-framework-docs`), handing it the propose + plan artifacts (`proposal.md`, `requirements/framework-docs.md`, `design.md`, `plan.md`) plus a deterministic spec for all three plan tasks. The coder implemented the two file edits, created `progress.md`, and committed; the orchestrator then verified independently and applied one consistency fix before final commit.

  **`docs/skills.md` — three coordinated edits (plan tasks 1 + 2):**
  - **Wayfinder entry** (lines 79–95): `### \`hamilton-wayfinder\` — chart the route before a change *(optional pre-change planning stage)*` inserted between `hamilton-init` (line 67) and `hamilton-propose` (line 97), matching the established When/Inputs/Produces/Notes/Source format. The Notes bullet carries the one-sentence rule (ticket 09) verbatim — "Use wayfinder to break a complex goal into clear, realizable units. Use `hamilton-propose` to transform each route unit into a concrete change spec ready for autonomous implementation." — AND the fork provenance in prose: "`hamilton-wayfinder` is a fork of upstream `mattpocock/skills` (MIT); see [`NOTICE`](../NOTICE) for the full legal credit." No licence text reproduced inline. When/Inputs/Produces grounded in `skills/hamilton-wayfinder/SKILL.md` (map at `.hamilton/maps/<effort>/`, the map plans the way / the doing comes later one change at a time). Source link points to `../skills/hamilton-wayfinder/SKILL.md`.
  - **Pipeline identity phrasing** (line 24): replaced "Seven skills." with "Six core skills in fixed sequence, plus an optional pre-change planning stage (wayfinder)." Rest of the paragraph and the ASCII diagram above it untouched. `grep 'Seven skills'` → 0 matches; `grep 'Six core skills'` → 1 match.

  **`CONTRIBUTING.md` — one mapping row (plan task 3):** added `| New/changed map artifacts in `.hamilton/maps/` | `docs/skills.md` |` at line 16, immediately after the existing wayfinder-templates row (line 15). Both rows present and distinct (`bundle/templates/wayfinder/` ≠ `.hamilton/maps/`), not merged. **Consistency fix applied by the orchestrator:** the coder's row initially omitted the backticks every other row uses around path surfaces; per CONTRIBUTING.md rule 3 ("Use the existing format... follow them") the path surfaces were wrapped in backticks (`.hamilton/maps/`, `docs/skills.md`) to match the table convention. `progress.md` Task 3 entry updated to reflect the final row.

  **Scope guard held literally:** `git diff --name-only HEAD~1..HEAD` → only `docs/skills.md`, `CONTRIBUTING.md`, and the new `.hamilton/changes/2026-08-09-sync-framework-docs/progress.md`. No `README.md`, no `docs/sdd-framework.md`, no code/tests. Gates green: `bun run build` exit 0; `bun --bun vitest run` 24/24 passed (3 files). Committed as `193f34c` (amended from the coder's `7214c18` to fold in the consistency fix — unpushed, one clean commit per task, matching the branch cadence). Branch `unit-09-sync-framework-docs` pushed to origin (`3bf05e7..193f34c`, fast-forward). **Next step is `hamilton-review`** (the following task).

- [x] Run `hamilton-review` on the unit's diff, confirming the scope was held literally (no touches to `README.md` or `docs/sdd-framework.md`) and the `docs/skills.md` entry matches the established format. Address any blocking findings.

  **Review complete — 0 images analyzed.** Ran `hamilton-review` in the unit-09 worktree (`.worktrees/unit-09-sync-framework-docs`, branch `unit-09-sync-framework-docs`), reviewing the full unit diff (`d57c01f..193f34c`, merge-base `d57c01f` on `port-wayfinder-siblings`) against `plan.md`, `requirements/framework-docs.md`, and `design.md`. **Verdict: approved** — 0 blocking, 0 suggestions. Wrote `.hamilton/changes/2026-08-09-sync-framework-docs/review.md` and appended the one-line summary to `progress.md`; committed as `5743eaf` and pushed `unit-09-sync-framework-docs` to origin.

  **All six requirements verified:**
  - **Req 1** — wayfinder entry (`docs/skills.md:79-95`) sits between `hamilton-init` (line 67) and `hamilton-propose` (line 97), in the established When/Inputs/Produces/Notes/Source format. Heading carries the `*(optional pre-change planning stage)*` step tag.
  - **Req 2** — the ticket-09 one-sentence rule is in the Notes bullet (lines 91-93). Substance verbatim; leading "Use" lowercased to "use" as a grammatical continuation after the `**Notes:**` colon, and `hamilton-propose` backticked to match the doc's convention (every other skill name is backticked). Both adaptations preserve meaning and follow the file's own formatting — accepted, not a defect.
  - **Req 3** — fork provenance in prose (lines 93-94): names upstream `mattpocock/skills` (MIT) and links to [`NOTICE`](../NOTICE). No licence text reproduced inline. `../NOTICE` resolves correctly from `docs/`.
  - **Req 4** — pipeline identity phrasing (line 24): "Seven skills." → "Six core skills in fixed sequence, plus an optional pre-change planning stage (wayfinder)." `grep 'Seven skills'` → 0; `grep 'Six core skills'` → 1. The "six" is arithmetically sound — they map to the unchanged diagram's main line (`init → propose → plan → code → review → finish-work`); wayfinder is the +1 optional pre-change stage, not a seventh core skill. Rest of the paragraph and the ASCII diagram untouched.
  - **Req 5** — CONTRIBUTING.md mapping row (line 16): `| New/changed map artifacts in `.hamilton/maps/` | `docs/skills.md` |`, immediately after the existing wayfinder-templates row (line 15, `bundle/templates/wayfinder/`). Both rows present and distinct, not merged; path surfaces backticked per the table's convention.
  - **Req 6** — scope held literally: `git diff --name-only d57c01f..HEAD` → only `CONTRIBUTING.md`, `docs/skills.md`, and this change's own `.hamilton/changes/` artifacts. `README.md` and `docs/sdd-framework.md` confirmed absent from the diff (grep exit 1). The six-skill diagram in both is untouched.

  **Gates re-run by the reviewer (evidence over claims):** `bun run build` → exit 0; `bun --bun vitest run` → 24/24 passed (3 files). Structural quality: mechanical docs-only change, no code, the `references/code-quality.md` rubric trips no smell (DRY holds — pipeline identity stated once in prose, not duplicated in the diagram; no dead code, stubs, or scope creep). **Next step is `hamilton-finish-work`** (the following task) with the `local-merge` strategy into `port-wayfinder-siblings`.

- [x] Run `hamilton-finish-work` with the **`local-merge`** strategy, merging the unit's branch back into **`port-wayfinder-siblings`** (not `main`), from the `port-wayfinder-siblings` worktree. Tear down the unit worktree after the merge; confirm the spec sync and `progress.md` finish entry ride into `port-wayfinder-siblings`.

  **Finish complete — 0 images analyzed.** Ran `hamilton-finish-work` with the `local-merge` strategy, closing change `2026-08-09-sync-framework-docs`. The spec sync, finish entry, and merge were done in two phases per the skill: spec + record **inside the unit worktree** (so they ride in on the merge), merge + teardown **from the `port-wayfinder-siblings` main checkout**.

  **Preconditions (unit worktree, branch `unit-09-sync-framework-docs`):** tree clean; `bun run build` → exit 0; `bun --bun vitest run` → 24/24 passed (3 files); latest `review.md` verdict `approved` (0 blocking, 0 suggestions); every plan task implemented per `progress.md`.

  **Spec sync — `framework-docs` created (first-time tracking).** No `.hamilton/specs/framework-docs.md` existed; the design (Decision 5) explicitly deferred canonical-spec creation to finish-work. Distilled `proposal.md` + `design.md` + `requirements/framework-docs.md` into the canonical skeleton (`## Overview` / `## Contract` + `### Skill entry shape` / `### Pipeline identity` anchors / `## Behavior` + Examples / `## Invariants` / `## Decisions`) per `references/spec-altitude.md`. Stated at altitude — no `SHALL`/`Requirement:`/`Scenario:` blocks, no delta-group headers; WHEN/THEN scenarios folded into a greppable Examples block; design decisions distilled to reusable rules (entry order groups by lifecycle; provenance in Notes not a subsection; phrasing replaces the identity sentence not the diagram; related mapping rows adjacent-but-distinct; the six-skill diagram in README/sdd-framework deliberately not redrawn; docs verified by reading not automated tests). Fork provenance and the literal-scope guard survive as invariants/decisions.

  **Finish entry:** appended `## Finish — 2026-08-09` to `progress.md` (preconditions, specs synced, local-merge into port-wayfinder-siblings, worktree removed). Committed spec + finish entry on the unit branch as `4d4a1a2` ("MAESTRO: Phase-04 task 7 — finish-work syncs framework-docs spec, records finish entry").

  **Local-merge (from the `port-wayfinder-siblings` worktree):** merge-base `d57c01f`; the only `port-wayfinder-siblings`-only commit (`1ebbc31`, task 2) touched the Phase-04 playbook doc alone — zero overlap with the unit's files, so the merge was clean. `git merge --no-ff unit-09-sync-framework-docs` → merge commit `2b8e5b5` ("MAESTRO: Merge unit-09-sync-framework-docs into port-wayfinder-siblings"). Confirmed the spec sync and the `progress.md` finish entry rode into `port-wayfinder-siblings`: `.hamilton/specs/framework-docs.md` present; `grep '## Finish — 2026-08-09' progress.md` → 1. Scope guard held: `git diff --name-only HEAD~1..HEAD` lists only the change artifacts, the new spec, `CONTRIBUTING.md`, and `docs/skills.md` — `README.md` and `docs/sdd-framework.md` absent.

  **Teardown:** worktree `.worktrees/unit-09-sync-framework-docs` removed (`git worktree remove`, from the main checkout — the skill forbids removing a worktree from inside it). Local branch `unit-09-sync-framework-docs` deleted (`-D`; `4d4a1a2` is merged into HEAD, so no data loss — the `-d` refusal was only because the branch sat one commit ahead of its remote tracking ref). Remote branch `origin/unit-09-sync-framework-docs` left in place, matching the unit-8 precedent (`origin/unit-08-propose-reads-route` was likewise left). `git worktree list` confirms no unit-09 worktree remains.

  **Next step is task 8** (the following checkbox) — verify the unit shipped: flip route.md §9 `Status:` to `shipped` and re-run the repo gates.

- [x] Verify the unit shipped: in `.hamilton/maps/hamilton-wayfinder/route.md` the `### 9.` section's `Status:` line now reads `shipped`. Run the repo gates — `bun run build` and `bun --bun vitest run` — and confirm both pass.

  **Unit 9 verified shipped — 0 images analyzed.** Flipped route.md §9 `Status:` from `pending` to `shipped` (line 241) directly on `port-wayfinder-siblings`, matching the unit-8 precedent (`cfd3f8b` flipped §8 the same way as a standalone commit on the integration branch). Section 10 (line 261) and the map-level status (line 3) remain `pending` — unit 10 is a separate phase and the map is only complete when all units ship. Confirmed via `rg -n 'Status:' route.md`: lines 41–241 now all read `shipped` (units 1–9), line 261 reads `pending` (unit 10), line 3 reads `pending` (map-level).

  **Gates green:** `bun run build` → exit 0; `bun --bun vitest run` → 24/24 passed (3 files). No code changed this commit — docs/map-only — so the gate re-run is the confirmatory check, not a regression guard. Committed as a standalone `MAESTRO:` commit on `port-wayfinder-siblings` and pushed. **Phase 04 is complete; all eight tasks are checked off.** The next phase (Phase 05) converts the map's own files to the mechanics contract (route unit 10).
