# Phase 03: Teach propose to read a route (Route Unit 8)

Give `hamilton-propose` map-aware entrypoint logic: when pointed at a map folder, it reads `route.md`, finds the next unit with status `pending`, and navigates that unit's decision links to pull full context before its normal dialogue begins. Everything after the entrypoint is unchanged. This is a contained, mechanical addition to an existing skill's entrypoint, so it uses the **coder** agent (not oracle). It must run after Phase 02 because both edit `skills/hamilton-propose/SKILL.md` — serializing them avoids a conflict on the same file. Once this unit ships, propose can read routes on its own, which the later units benefit from.

## Tasks

- [x] Load the unit's context. Read the `### 8. Teach propose to read a route` section of `.hamilton/maps/hamilton-wayfinder/route.md` and its two backing tickets: `tickets/09-boundary-with-propose-and-critique.md` (the map-aware behaviour spec — propose reads the folder, finds `route.md`, identifies the next `pending` unit, navigates decision links to pull context) and `tickets/13-map-artifacts-and-worktrees.md` (the branching rule — propose reads `route.md` from the branch the session started on and does **not** reach for the default branch's copy, which keeps propose's worktree gate intact). Also read the current `skills/hamilton-propose/SKILL.md` to locate where the entrypoint logic lands (step 1 / step 3 context-exploration).

  **Context loaded.** Route unit 8 (`route.md:222-237`) is `pending`, depends on units 6 + 7 (both `shipped`). Ticket 09 fixes the behaviour: the human points propose at a map folder; propose reads the folder, finds `route.md`, identifies the next `pending` unit, navigates that unit's decision links to pull full context, then proceeds into its normal workflow unchanged. Propose is a required gate — no straight-to-plan path. Ticket 13 fixes the branching rule: propose reads `route.md` from the branch the session started on (never the default branch's copy), which keeps propose's worktree gate intact; status flips ride the unit's own branch.

  **Entrypoint landing points in `skills/hamilton-propose/SKILL.md`:**
  - **Step 1** (Derive the title, ensure an isolated workspace) — map-folder detection happens here; the title derives from the next pending unit's slug rather than a free-form request.
  - **Step 3** (Explore context, read-only) — reading `route.md` (current branch) and navigating the next pending unit's decision links into `tickets/` feeds the existing context-exploration step. Steps 4–10 stay untouched.

- [x] Establish an isolated workspace for this unit off `port-wayfinder-siblings`: create a worktree on a dedicated branch (e.g. `unit-08-propose-reads-route`) and `cd` into it, confirming `git rev-parse --show-toplevel` ends in that worktree.

  **Workspace established — 0 images analyzed.** Created worktree at `/Users/caio.cavalcante/personal/hamilton/.worktrees/port-wayfinder-siblings/.worktrees/unit-08-propose-reads-route` on branch `unit-08-propose-reads-route`, based off `port-wayfinder-siblings` (`4a8079d`, the tip that includes unit-7's merged grilling refactor). Confirmed `git rev-parse --show-toplevel` → `…/.worktrees/unit-08-propose-reads-route`, `branch --show-current` → `unit-08-propose-reads-route`, base commit `4a8079d`, and `git rev-parse --git-dir` ≠ `--git-common-dir` (linked worktree).

  **Path note — deliberate deviation from unit-7's sibling worktree.** Unit-7 placed its worktree as a sibling at `…/.worktrees/unit-07-…`, which sat outside this agent's hard write boundary and forced the Auto Run `Working/`-folder staging dance for `review.md`/`progress.md`. Unit-08 nests under `port-wayfinder-siblings/.worktrees/unit-08-propose-reads-route` instead, keeping the worktree **inside** the `port-wayfinder-siblings/` write boundary. Every subsequent task in this phase can therefore write artifacts directly to the change dir on the `unit-08-propose-reads-route` branch — no staging, no subagent workaround for boundary tension. Ticket 13's rule is fully satisfied (unit rides its own branch; the status flip rides that branch; propose reads `route.md` from the branch the session started on) — only the on-disk path of the worktree differs, which ticket 13 does not constrain. `.worktrees/` is gitignored, so the nested worktree does not pollute `port-wayfinder-siblings`'s `git status` (verified: parent status shows only the pre-existing untracked Phase-03–07 playbook docs, not the worktree).

  Subsequent unit-08 work runs from this worktree: `cd .worktrees/unit-08-propose-reads-route` from the `port-wayfinder-siblings` worktree root (or set the workdir there). Branch `unit-08-propose-reads-route` is local for now; it pushes with the first artifact commit, matching the unit-7 cadence.

- [x] Run `hamilton-propose` for this unit, feeding it the unit 8 goal and the ticket 09 / 13 decisions as the change request (propose does not yet read routes — that is exactly what this unit adds — so supply context explicitly). The proposal must scope the change to the entrypoint only: when given a map folder, read `route.md`, find the next `pending` unit, navigate its decision links, then proceed into propose's normal workflow unchanged. Answer any HITL questions from the ticket context so the phase is autonomous.

  **Propose artifacts produced — 0 images analyzed.** Ran the full `hamilton-propose` process (steps 1–10) inside the `unit-08-propose-reads-route` worktree, feeding the unit 8 goal from `route.md:222-237` and the decisions from tickets 09 + 13 as the change request. Since propose does not yet read routes (this unit adds that), context was supplied explicitly as the task instructs. All HITL questions answered from ticket context — the proposal's Open Questions section reads "None" with citations to both tickets.

  **Artifacts committed** to `unit-08-propose-reads-route` at `a9fde53`:
  - `.hamilton/changes/2026-08-09-propose-reads-route/proposal.md` — scopes the change to two additive edits in `skills/hamilton-propose/SKILL.md`: step 1 (map-folder detection, route reading, title derivation from unit slug) and step 3 (decision-link navigation). Steps 4–10 explicitly unchanged. Non-goals: no tests, no other skills touched, no CLI/template changes, no canonical spec for propose.
  - `.hamilton/changes/2026-08-09-propose-reads-route/requirements/propose.md` — five ADDED requirements: map-folder detection, route read from session's starting branch (SHALL NOT reach for default branch), next pending unit identification, decision-link navigation, steps 4–10 unchanged. Each with WHEN/THEN scenarios.
  - `.hamilton/changes/2026-08-09-propose-reads-route/design.md` — five decisions: detection+title in step 1 / context in step 3 (rejected step-0 and monolithic alternatives); map-aware mode as a conditional branch (rejected dual-flow); route read from working tree not via git (ticket 13); unit status scanned from `Status:` line; no-pending-unit edge case stops with a message. Quality lens clears all five code-quality principles.

  **Self-review passed.** Workspace confirmed (`git rev-parse --show-toplevel` → `…/.worktrees/unit-08-propose-reads-route`); no placeholders, contradictions, or scope creep; code-quality rubric applied. Approval step (10) recorded as autonomous — no open questions, tickets resolve every decision.

- [x] Run `hamilton-plan` on the approved propose artifacts to produce the ordered task ledger for the entrypoint addition.

  **Plan produced — 0 images analyzed.** Ran the full `hamilton-plan` process inside the `unit-08-propose-reads-route` worktree (isolation confirmed: linked worktree, branch `unit-08-propose-reads-route`, not `main`). Read the approved propose artifacts (`proposal.md`, `requirements/propose.md`, `design.md`), the `references/code-quality.md` rubric, the `~/.hamilton/templates/plan.md` template, the current `skills/hamilton-propose/SKILL.md` (to locate the exact splice points in steps 1 and 3), and the route format (`bundle/templates/wayfinder/route.md` + `.hamilton/maps/hamilton-wayfinder/route.md`) to confirm the `### N.` / `Status:` / `Backed by:` shape the detection logic scans. Confirmed no canonical spec exists for `propose` (the requirements delta is first-time tracking, matching the unit-7 precedent).

  **`plan.md` written** to `.hamilton/changes/2026-08-09-propose-reads-route/plan.md` and committed at `c4095d8` (pushed to `origin/unit-08-propose-reads-route`). Three TDD-sized tasks, ordered along the design's step boundary:
  - **Task 1** — add map-folder detection + title derivation to step 1 (map-aware conditional: read `route.md` from the working tree, SHALL NOT reach for the default branch's copy per ticket 13, scan `### N.` units for the first `pending`, derive kebab-case title from the unit name, stop with a message if none pending). Satisfies propose requirements #1, #2, #3.
  - **Task 2** (depends on 1) — extend step 3 to navigate the selected unit's `Backed by:` decision links, reading each `tickets/NN-slug.md`; no-backing-tickets edge falls back to the goal paragraph. Satisfies requirement #4.
  - **Task 3** (depends on 2) — end-to-end coherence read (map-aware branch merges at step 4; steps 4–10 byte-identical) + repo gates (`bun run build`, `bun --bun vitest run`). Satisfies requirement #5.

  Each task carries the exact step text to splice (modeled as the clean conditional-branch shape, copied verbatim by the coder) and a concrete verify. Self-review passed: workspace correct, all five requirement scenarios mapped to task acceptance, dependencies acyclic, no structural smells. Next step is `hamilton-code` (Task 1) — the coder dispatch.

- [x] Dispatch the **coder** agent to implement the change in `skills/hamilton-propose/SKILL.md`, giving it the propose + plan artifacts. The entrypoint logic must: detect when it is pointed at a `.hamilton/maps/<effort>/` folder; read `route.md` from the current branch (the branch the session started on — never reach for the default branch's copy, per ticket 13); locate the next unit whose status is `pending`; navigate that unit's backing decision links into `tickets/` to pull full context; then hand off to propose's existing workflow. Everything after the entrypoint stays untouched.

  **Coder dispatched — 0 images analyzed.** Dispatched the coder agent against the `unit-08-propose-reads-route` worktree with the plan's exact splice text. The coder implemented all three plan tasks:

  - **Task 1** (`0b13639` — `MAESTRO: add map-folder detection and title derivation to propose step 1`): replaced step 1's opening sentence `Derive a kebab-case title from the request.` with the map-aware conditional branch — detect `.hamilton/maps/<effort>/` + `route.md`, read `route.md` from the working tree (explicit SHALL NOT on `git show`/`git checkout`/branch-switch per ticket 13), scan `### N.` units for the first `Status: pending`, derive the title from the unit's name, stop with a message if none pending, free-form fallback otherwise — rejoining the existing isolation logic at `Then detect isolation:`.
  - **Task 2** (`ae87e81` — `MAESTRO: extend propose step 3 to navigate the selected unit's decision links`): inserted the map-aware navigation sentence into step 3 between `... rather than contradicting it.` and `If the request spans several independent subsystems,` — reads the selected unit's `Backed by:` ticket links (`tickets/NN-slug.md`), feeds them into context exploration, with a no-`Backed by:` fallback to the route entry's goal paragraph.
  - **Task 3** (no commit — coherence read clean): the end-to-end read found no non-sequitur, dangling reference, or contradiction. Steps 4–10 are byte-identical to their pre-change state (confirmed via `git diff HEAD~2 HEAD` — only two hunks: step 1 splice, step 3 splice). No fix was needed, so no empty commit was created.

  **Verification (re-run by this agent):** `bun run build` exits 0; `bun --bun vitest run` passes 3 files / 24 tests. Working tree clean. Commits pushed to `origin/unit-08-propose-reads-route`. The entrypoint is additive and the rest of propose's workflow reads coherently with the new front logic — ready for `hamilton-review`.

- [x] Run `hamilton-review` on the unit's diff, confirming the entrypoint is additive and the rest of propose's workflow reads coherently with the new front logic. Address any blocking findings.

  **Review approved — 0 images analyzed.** Ran the full `hamilton-review` process inside the `unit-08-propose-reads-route` worktree (isolation confirmed: linked worktree, branch `unit-08-propose-reads-route`, not `main`). Established intent from `proposal.md`, `design.md` (5 decisions + Quality Lens), `requirements/propose.md` (5 ADDED), and `plan.md` (3 tasks); no `progress.md` existed from the coder, so this review established the first progress entry.

  **Diff inspected in full.** `git diff 4a8079d..ae87e81` — the coder's two commits touched one file (`skills/hamilton-propose/SKILL.md`, +18/-4) in exactly two hunks: step 1 (map-folder detection + title derivation) and step 3 (decision-link navigation). Steps 4–10 and every surrounding section (Principles, Inputs, References, Process flow, Output, Handoff) are byte-identical to the pre-change state — the entrypoint-only guarantee holds.

  **All 5 propose requirements satisfied** — map-folder detection + free-form fallback (#1); route read from the working tree with ticket 13's SHALL NOT stated verbatim (#2); next-`pending`-unit scan + title from the unit's name + no-pending-unit stop (#3); `Backed by:` decision-link navigation + no-backing-tickets fallback, conditional on step 1's mode (#4); steps 4–10 byte-identical, map-aware branch merges back at "Then detect isolation:" and the decompose guard so step 4 reads coherently with the new front logic (#5). The scanned format (`### N.`, `Status:`, `Backed by:`) matches the route template and the worked example this route is. End-to-end read of the full 211-line skill found no non-sequitur, dangling reference, or contradiction.

  **Gates green.** `bun run build` → exit 0; `bun --bun vitest run` → 24/24 pass (3 files). No tests required (ticket 09: `skills/` is not bundled, no test asserts on skill content — unit-7 precedent). Security N/A (prose edit, no secrets/code/input handling). Structural quality clears the rubric: single responsibility, DRY conditional branch, low coupling to route.md's public format, right-sized (two sentences, no framework), open for extension.

  **Verdict: approved (blocking: 0, suggestions: 1).** Artifacts committed at `17cc5ff` (pushed to `origin/unit-08-propose-reads-route`): `.hamilton/changes/2026-08-09-propose-reads-route/review.md` + `progress.md`. One non-blocking suggestion — step 1's "Otherwise derive a kebab-case title from the request" sits right after the no-pending-unit stop; the disambiguating clause makes it mean the non-map-folder case, but leading the free-form fallback more explicitly would remove any momentary ambiguity. Deferred to the mandated `writing-great-skills` pass (next task). Next step: `writing-great-skills` craft pass on `skills/hamilton-propose/`, then `hamilton-finish-work` (local-merge into `port-wayfinder-siblings`).

- [x] Run the `writing-great-skills` skill **explicitly** against `skills/hamilton-propose/` (it was edited again this unit) and fix every finding in place — the mandated post-unit craft pass for the skill this phase changed.

  **Craft pass complete — 0 images analyzed.** Ran `writing-great-skills` against `skills/hamilton-propose/` (whole skill, focus on the two hunks this unit added: step 1 map-aware block, step 3 navigation sentence). Five findings, all in the changed prose, fixed in place at `acdfbdd` (pushed to `origin/unit-08-propose-reads-route`):

  - **Negation (step 1).** `do not reach for the default branch's copy via \`git show\`, \`git checkout\`, or any equivalent` enumerated the forbidden commands — naming the elephant and making them more available. Replaced with a positive-led clause: the working tree's copy is the session's copy (worktree is based off the current branch), with a tight trailing guardrail `never the default branch's` — ticket 13's SHALL NOT preserved without the command list.
  - **Negation (step 1).** `do not fall through to free-form mode` named the forbidden path; the positive `stop and tell the user` already binds. Dropped.
  - **Duplication (step 1).** `the branch the session started on` restated the parenthetical rationale (`worktree … based off the current branch, so the working tree's copy is the session's copy`). Collapsed to the single rationale.
  - **Ambiguity — review's deferred suggestion (step 1).** `Otherwise derive a kebab-case title from the request` sat right after the no-pending stop, leaving momentary doubt whether *Otherwise* was else-of-stop or else-of-map-folder. Restructured: lead with the free-form default (`Derive a kebab-case title from the request — unless …`), branch map-aware as the explicit exception, and the no-pending stop is now the terminal *inside* that exception. No `Otherwise` remains; the free-form fallback is led explicitly.
  - **Duplication (step 3).** `alongside the specs, docs, and recent commits` restated step 3's opening enumeration; `backing decision links — the tickets listed in its \`Backed by:\` line —` was a redundant gloss. Tightened to `the selected unit's \`Backed by:\` links — reading each linked \`tickets/NN-slug.md\` … — and feed it into this exploration.` (positive no-`Backed by:` fallback kept).

  **Scope respected.** Only the two hunks this unit introduced were touched; pre-existing prose (e.g. `rather than contradicting it`) was left for a future whole-skill pass to avoid expanding the diff past the unit's entrypoint-only guarantee. The `map-aware mode` branch label (coined this unit) is reused consistently across step 1 and step 3 and kept — it anchors the conditional branch.

  **Gates green.** `bun run build` → exit 0; `bun --bun vitest run` → 24/24 pass (3 files). `skills/` is not bundled, so the prose edit does not affect the gates (unit-7 precedent). Diff is two hunks, +11/−14 against the coder's tip `ae87e81`; steps 4–10 and all surrounding sections remain byte-identical.

- [x] Run `hamilton-finish-work` with the **`local-merge`** strategy, merging the unit's branch back into **`port-wayfinder-siblings`** (not `main`), from the `port-wayfinder-siblings` worktree. Tear down the unit worktree after the merge; confirm the spec sync and `progress.md` finish entry ride into `port-wayfinder-siblings`.

  **Finish-work complete — 0 images analyzed.** Ran the full `hamilton-finish-work` process from the `port-wayfinder-siblings` worktree (the merge target). Preconditions all held on the `unit-08-propose-reads-route` branch: working tree clean, `bun run build` exit 0, `bun --bun vitest run` 24/24 pass (3 files), review verdict `approved` (0 blocking), all plan tasks implemented.

  **Spec sync — `propose` created (first-time tracking).** No `.hamilton/specs/propose.md` existed; the 5 ADDED map-aware-entrypoint requirements were distilled into the canonical skeleton (`## Overview` / `## Contract` / `## Behavior` + Examples / `## Invariants` / `## Decisions`) at altitude per `references/spec-altitude.md`. The route-from-working-tree invariant was lifted to the effect ("MUST NEVER reach for the default branch's copy") rather than the `git show`/`git checkout` enumeration — matching the lift the skill's own `writing-great-skills` pass already applied. Committed at `d2734d9` on the unit branch.

  **Finish entry recorded** in `progress.md` (craft-pass entry + `## Finish — 2026-08-09` block) and committed at `c27f9fc` on the unit branch **before** the merge, so it rode into the base branch with the rest of the change.

  **local-merge.** `git merge --no-ff unit-08-propose-reads-route` into `port-wayfinder-siblings` → merge commit `1b256e8` (8 files, +546/−4). Brings in the change artifacts (`proposal.md`, `design.md`, `requirements/propose.md`, `plan.md`, `review.md`, `progress.md`), the canonical spec (`.hamilton/specs/propose.md`), and the entrypoint edit (`skills/hamilton-propose/SKILL.md`). Confirmed the spec sync and the `progress.md` finish entry are present at `HEAD` on `port-wayfinder-siblings`. Merge-commit pattern matches unit-7's `f82f859`.

  **Teardown.** `git worktree remove .worktrees/unit-08-propose-reads-route` (exit 0) — the nested worktree is gone (`git worktree list` no longer lists it). Local `unit-08-propose-reads-route` branch deleted (merged into HEAD; `git branch -D`, matching unit-7's delete-local-leave-remote precedent — `origin/unit-08-propose-reads-route` remains).

  **Gates re-run on the merged `port-wayfinder-siblings` tree** as a sanity check: `bun run build` exit 0; `bun --bun vitest run` 24/24 pass (3 files). (The formal route-status-flip + gate verification is the next task.) Next step: flip `### 8.` status to `shipped` in `route.md` and run the gates — task 9.

- [x] Verify the unit shipped: in `.hamilton/maps/hamilton-wayfinder/route.md` the `### 8.` section's `Status:` line now reads `shipped`. Run the repo gates — `bun run build` and `bun --bun vitest run` — and confirm both pass.

  **Unit 8 shipped — 0 images analyzed.** Confirmed the unit-08 work merged into `port-wayfinder-siblings` via merge commit `1b256e8` (`git log --oneline` shows it at `HEAD~1`), bringing in `skills/hamilton-propose/SKILL.md` (+19/-4), `.hamilton/specs/propose.md` (first-time canonical spec), and the change artifacts under `.hamilton/changes/2026-08-09-propose-reads-route/`. Flipped the `### 8.` section's `Status:` line from `pending` → `shipped` in `.hamilton/maps/hamilton-wayfinder/route.md` (only that one token changed; `Depends on: 6, 7` and the rest of the section untouched). The status flip rides this branch, satisfying ticket 13's "status flips ride the unit's own branch" rule.

  **Gates green on `port-wayfinder-siblings` at HEAD.** `bun run build` → exit 0; `bun --bun vitest run` → 24/24 pass (3 files). Unit 8 is fully shipped. Route units 9 and 10 remain `pending`; this phase (Phase 03) is complete.
