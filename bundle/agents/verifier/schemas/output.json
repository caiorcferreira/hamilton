{
  "type": "object",
  "required": ["status"],
  "properties": {
    "status": { "type": "string", "enum": ["done", "failed"] },
    "verified": { "type": "string" },
    "feedback": { "type": "string" }
  }
}
