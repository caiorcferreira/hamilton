# Whole-branch Review: Separate task execution and feedback from whole-branch review

## Pass 1 — 2026-09-05

Base: 8b711317796319834365411e4d1c48fd42e1971c
Head: f3fe6b1255c4b65cd8cfb66d5ed3d6970f9cbc9d
Verdict: changes-requested

### Blocking

- [`.hamilton/changes/2026-09-04-split-hamilton-review/tasks/task-1/feedback.md:3`; `.hamilton/changes/2026-09-04-split-hamilton-review/tasks/task-2/feedback.md:5`] [P1] The branch advanced through all implementation tasks before committing their tactical feedback: Task 1's pass reviewing `ea7a8e4a63ccaa038ceecf6a740c0ba4568e0bfe..f284efa6a8e394abb65847ed79da3c219551e73b` was not committed until `018f846783826d7c21d1a38814f35deb809bee5a`, after Task 14's implementation head `dba72e1d277d1efae54b44921ea9a3364947c0da`, while Task 2's recorded base is Task 1's implementation head rather than a subsequent feedback commit. Tasks 1–13 have the same backfilled ordering. Rebuild the evidence sequence so each artifact-only feedback commit precedes the next task checkpoint, or obtain and record an explicit upstream bootstrap exception before re-review; appending another verdict cannot retroactively satisfy the approved sequencing contract. (violates: `plan.md` context notes; `requirements/review.md` task-feedback persistence and orchestration-order requirements; `design.md` feedback-before-next-checkpoint invariant)

- [`bundle/scripts/hamilton-precondition-check.sh:56`; `bundle/scripts/hamilton-precondition-check.sh:630`] [P1] The finish gate can accept approval artifacts that do not exist in committed history because its cleanliness check omits ignored files and the review gate reads feedback and review directly from the filesystem without proving they are tracked at `HEAD`. A focused temporary-repository check removed Task 1 feedback from the index, ignored and recreated the valid file, observed `status-count: 0`, and still produced `[PASS] Reviews` and `gate: open`. Require every plan, ledger, active task progress/feedback file, and root review consumed by the gate to be tracked at `HEAD` and backed by committed content, with regressions for ignored uncommitted verdicts. (violates: `requirements/review.md` task and whole-branch artifact-only commit requirements; `design.md` explicit uncommitted-verdict stop)

- [`bundle/scripts/hamilton-precondition-check.sh:760`] [P1] Cleanliness is checked before the configured verification command and never checked again, so a successful test command can dirty a tracked source and the gate still opens. A focused fixture used a passing command that appended to `README.md`; the helper printed `[PASS] Clean tree`, `[PASS] Tests`, and `gate: open` while post-gate status was ` M README.md`. Recheck the target repository immediately after verification and again at gate entry, and cover a zero-exit verification command that changes a tracked file. (violates: `requirements/review.md` immediate pre-finish freshness/cleanliness gate; `design.md` finish-gate component contract; `plan.md` non-waivable cleanliness acceptance)

- [`bundle/scripts/hamilton-diff-package.sh:49`; `bundle/scripts/hamilton-precondition-check.sh:734`] [P1] `--change-dir` does not bind Git operations or verification to that directory's repository: diff-package resolves the directory but uses caller-CWD Git state for checkpoint, ignore, ancestry, and diff operations, while precondition checks cleanliness and runs tests in caller CWD before deriving a separate repository for review ancestry. A focused two-repository fixture left the target repository dirty, invoked the gate from this clean repository, and obtained `[PASS] Clean tree`, `[PASS] Tests`, and `gate: open`. Resolve one repository root from the change directory and use it for every Git and test operation, or reject a caller-CWD mismatch; add two-repository tests for both helpers. (violates: `requirements/execution.md` task checkpoint ownership; `requirements/review.md` finish gate; `design.md` helper interfaces and cleanliness boundary)

- [`bundle/scripts/hamilton-precondition-check.sh:487`; `.hamilton/changes/2026-09-04-split-hamilton-review/tasks/task-7/feedback.md:15`] [P1] The finish parser treats any occurrence of the words `cannot verify from diff` anywhere in a pass as an unresolved item. Task 7's approved Suggestions entry uses those words only to describe verified regression coverage, so a focused gate run with an otherwise valid root approval reports `Task 7(unresolved cannot verify from diff)` and closes. Parse unresolved items from their canonical blocking semantics rather than substring-searching arbitrary prose, and add positive coverage for a statement about resolved/tested `cannot verify from diff` behavior. (violates: `requirements/review.md` unresolved-impact and all-review-gates-current scenarios; `plan.md` Done when)

- [`bundle/scripts/hamilton-change-context.sh:416`; `bundle/scripts/hamilton-precondition-check.sh:406`] [P1] Verdict-history validation is neither exact nor consistent. Change-context validates only one H1/H2 plus Base, Head, and Verdict, so it reports a fresh approval when both required findings sections are absent or when approval carries a blocking item; neither helper validates contiguous, unique pass numbering. Focused fixtures made context report Task 14 `approved (fresh)` for a sectionless `Pass 2`, and made precondition report `[PASS] Reviews`, `[PASS] Whole-branch review freshness`, and `gate: open` when root review began at `Pass 2`. Enforce one canonical fail-closed pass grammar in both consumers, including identity, complete ordered sections, list semantics, contradictions, unresolved-item semantics, and contiguous physical numbering, with no fallback to an earlier pass. (violates: `requirements/review.md` task-feedback and whole-branch verdict-history requirements; `skills/hamilton-code-feedback/SKILL.md` and `skills/hamilton-review/SKILL.md` pass-numbering contracts)

- [`bundle/scripts/hamilton-change-context.sh:529`] [P2] Whole-branch material freshness excludes the broad globs `tasks/task-*/{progress,feedback}.md`, so a noncanonical path such as `tasks/task-not-a-task/feedback.md` is incorrectly treated as bookkeeping. A focused fixture produced `whole change: approved (fresh)` from context while precondition correctly reported that the same review did not contain the noncanonical material commit. Build exclusions only from exact numeric `task-N` owners under the defined active/history policy and mirror precondition's noncanonical-path regression in context tests. (violates: `requirements/review.md` exact material-path definition; `design.md` whole-branch freshness decision)

- [`bundle/scripts/hamilton-change-context.sh:273`; `bundle/scripts/hamilton-precondition-check.sh:282`; `.hamilton/changes/2026-09-04-split-hamilton-review/tasks/task-1/progress.md:75`; `.hamilton/changes/2026-09-04-split-hamilton-review/tasks/task-4/progress.md:9`] [P1] The new split-layout consumers deliberately accept the removed legacy `## Task N: <title> — <date>` attempt syntax alongside canonical `## Attempt N — <date>`, and the current Task 1 and Task 4 histories depend on that undocumented fallback. This makes a partially converted change pass as split, loses monotonic attempt identity, and contradicts the selected no-compatibility migration. Remove the alternate parser branch and its legacy-acceptance fixture so only installed `task-progress.md` syntax is valid; because these committed task histories are append-only, resolve the self-hosting conflict through explicit upstream approval or a rebuilt bootstrap rather than preserving a hidden parser exception. (violates: `requirements/artifact-templates.md` task-progress shape; `requirements/execution.md` Legacy formats are inventoried but never interpreted; `design.md` no-compatibility non-goal)

- [`skills/hamilton-code-feedback/SKILL.md:43`; `skills/hamilton-review/SKILL.md:46`] [P1] The direct tactical and whole-branch review entry points enforce scope but never enforce the atomic-generation boundary. Unlike plan, code, orchestration, and finish, they can proceed against a planned monolithic or partially scaffolded legacy change and append new-generation verdict artifacts into it. Add an early no-write generation gate to both skills and their contract tests: once `plan.md` exists, require the exact split ledger and all required active-task artifacts; otherwise stop as `legacy-unsupported` with the between-changes guidance. (violates: `requirements/execution.md` Legacy formats are inventoried but never interpreted; `design.md` inventory-without-support decision)

- [`skills/hamilton-orchestrate/SKILL.md:115`; `skills/hamilton-orchestrate/references/code-feedback-prompt.md:36`] [P1] The resume matrix sends every fresh `changes-requested` pass directly to code, including the distinct unresolved `cannot verify from diff` case that the later process text says the driver must adjudicate. The feedback dispatch template also has no placeholder for located cross-task evidence, so the promised same-Head evidence-only re-feedback path cannot be dispatched faithfully. Split the matrix into ordinary requested changes versus unresolved verification, route the latter through driver adjudication, and add an explicit located-evidence input to the prompt plus a contract test for satisfied-risk re-feedback at the unchanged head. (violates: `requirements/review.md` Unverified task impact is resolved before approval; `design.md` scope-specific prompt and edge-case decisions)

- [`skills/hamilton-code/SKILL.md:76`; `bundle/scripts/hamilton-diff-package.sh:147`] [P1] The direct code path always runs `--record` when a checkpoint is absent, while `cmd_record` writes current `HEAD` without checking whether the task already has implementation or feedback evidence. On a manual retry or correction whose ignored `.base` was lost, this silently rebases the review range and omits earlier task work; only orchestrate documents the required reconstruction/stop behavior. Make code apply the same evidence-free-first-attempt guard and historical recovery rules before record, or make the helper reject a missing checkpoint when durable task evidence exists; add direct-code recovery contract coverage. (violates: `requirements/execution.md` stable checkpoint and missing-checkpoint-on-resume scenarios; `design.md` stable checkpoint decision)

- [`bundle/scripts/hamilton-change-context.sh:76`; `bundle/scripts/hamilton-precondition-check.sh:85`; `bundle/scripts/hamilton-diff-package.sh:97`] [P2] Active-task parsing is not one exact fail-closed contract. Context and precondition classify any title containing `(abandoned` as abandoned instead of requiring the canonical suffix, while diff-package raw-greps unstripped plan text, accepts duplicate declarations, and can create a checkpoint for a task heading inside an HTML comment. Its `${title,,}` test is also unsupported by macOS's system Bash 3.2. Replace these divergent checks with portable comment-aware parsing that requires exactly one canonical positive task declaration and exact abandonment syntax, then add malformed-marker, commented-task, duplicate-id, and Bash-3-compatible regressions. (violates: `requirements/execution.md` exact active task/checkpoint ownership; `skills/hamilton-plan/SKILL.md` canonical abandonment syntax; `plan.md` exact-task and GNU/BSD portability constraints)

- [`skills/hamilton-code/SKILL.md:114`; `skills/hamilton-code-feedback/SKILL.md:115`; `skills/hamilton-review/SKILL.md:136`; `skills/hamilton-finish-work/SKILL.md:158`] [P1] The branch deletes the project-local template mirror but recreates four exact, prescriptive artifact shapes inside skill bodies, and the new skill tests lock those copies independently of `bundle/templates/`. Shape evolution therefore still requires synchronized edits in multiple repository locations even though the canonical specification says producers read the installed template instead of carrying a copy. Make each producer load and instantiate its installed named template, retain lifecycle and validation semantics rather than duplicate field/order syntax in the skill, and keep exact shape assertions at the bundled-template boundary. (violates: `requirements/artifact-templates.md` The bundle is the only repository template source; `.hamilton/specs/artifact-templates.md` single-definition invariant; `design.md` DRY/source-of-truth decision)

- [`bundle/templates/progress.md:1`; `bundle/templates/task-progress.md:9`; `bundle/templates/finish.md:17`; `.hamilton/changes/2026-09-04-split-hamilton-review/tasks/task-4/feedback.md:1`] [P1] The split templates conflict with unsuperseded canonical template invariants: none of the five changed lifecycle templates tells producers to delete its instructional opening block, and committed feedback instances consequently retain those instructions; task-progress also stubs an attempt that plan explicitly removes at creation, while finish stubs an outcome that does not exist when the attempt file is created. Add the required deletion instruction and ensure producers remove hints, then make templates represent only creation-time content with append shapes kept in the owning contract, or explicitly revise and approve the canonical invariant before implementing a different model. (violates: `.hamilton/specs/artifact-templates.md` template idiom and creation-time-shape decisions; `requirements/artifact-templates.md` installed task-progress and finish lifecycle requirements)

- [`skills/hamilton-code-feedback/SKILL.md:150`; `skills/hamilton-review/SKILL.md:176`] [P1] Both verdict producers check whether their bookkeeping commit is artifact-only only after creating it. Neither first rejects an unrelated staged path nor prescribes a path-limited commit, so pre-staged implementation or user work can be absorbed into the verdict commit; stopping after inspection leaves the required history already corrupted. Add a pre-commit index/path-set gate or a provably path-limited commit, retain post-commit verification, and cover unrelated pre-staged content in both contract tests. (violates: `requirements/review.md` task-feedback and whole-branch artifact-only commit requirements; `design.md` review-producer test obligation)

- [`README.md:69`; `README.md:99`; `docs/sdd-framework.md:211`; `docs/sdd-framework.md:258`; `docs/modes.md:47`; `docs/skills.md:312`] [P1] The migration guidance says to update skills, templates, and helpers but omits the required `hamilton setup` reinstall, while the same public docs claim every helper call has a colocated manual recipe and the pipeline works without setup. The new mandatory checkpoint/package calls in code and orchestration have no such fallback, so users can retain an incompatible installed generation or stop mid-pipeline. State the actual atomic upgrade procedure, including `hamilton setup` and verification of the installed generation, then either restore complete manual recipes at every call site or document helpers/setup as required; update the helper caller table to the actual consumers. (violates: `requirements/framework-docs.md` actionable between-changes migration and maintained skill-reference requirements; `design.md` Migration / Rollout)

- [`README.md:137`; `docs/sdd-framework.md:90`] [P2] Two pipeline summaries contradict the binding sequence they introduce: the quick start places finish-history recording before specification synchronization, although finish synchronizes and commits specs before recording Attempt N, and the framework says steps 1–6 run per change although propose/step 1 is optional. Correct the quick-start finish order and describe step 0 as once, step 1 as optional, and steps 2–6 as per-change. (violates: `requirements/execution.md` finish-history ordering; `requirements/framework-docs.md` seven-stage pipeline identity)

- [`bundle/templates/README.md:14`; `bundle/templates/README.md:24`; `bundle/templates/task-progress.md:4`; `docs/skills.md:164`] [P2] Maintained catalog text misstates artifact lifecycle: task progress is shown as written only by code although plan initializes every instance; `plan.md` is called the only required artifact and every downstream stage is said to work from a raw description although split operational scaffolding is mandatory; and the code entry promises an implementation commit even for a graceful blocked outcome that produces only bookkeeping. Update producer/updater ownership, distinguish the sole required declarative input from required operational artifacts, remove the obsolete “start anywhere” downstream claim, and qualify success versus blocked outputs. (violates: `requirements/artifact-templates.md` template catalog requirement; `requirements/framework-docs.md` artifact and contributor documentation requirement; `requirements/execution.md` blocked-attempt behavior)

### Suggestions

- None.

## Pass 2 — 2026-09-05

Base: 8b711317796319834365411e4d1c48fd42e1971c
Head: 596fe3f3d0b72b15e31d10d12e78bdfaa41472ee
Verdict: changes-requested

### Blocking

- [`bundle/scripts/hamilton-change-context.sh:464`; `bundle/scripts/hamilton-precondition-check.sh:403`] [P1] Whole-branch owner identity is still self-authenticating: context constructs the expected `Whole-branch Review:` heading from the review file's own first heading, and precondition extracts that same heading and passes it back as the parser's expected value. A review copied from another change therefore satisfies the canonical grammar and can be reported fresh or open the finish gate when its commit range is otherwise valid. Derive the expected change title from the owning plan/change artifact in both consumers, pass that independent identity to the shared parser, and add wrong-title and decorated-heading regressions. (violates: `plan.md` Task 18 exact-owner-H1 acceptance; `requirements/review.md` change-owned verdict-history requirement; `requirements/artifact-templates.md` whole-branch review instance ownership)

- [`bundle/scripts/hamilton-artifact-contracts.sh:111`] [P1] The shared verdict grammar rejects an `approved` pass with blocking findings but accepts the inverse contradiction, `Verdict: changes-requested` with `### Blocking` containing only `- None.`. A focused invocation returned `changes-requested` with blocking count `0`, so context treats an actionless pass as valid and orchestration can send a task back to code without any correction to perform. Require `changes-requested` to carry at least one canonical Blocking finding and forbid `None.` in that case, then apply the same regression matrix through both context and precondition consumers. (violates: `plan.md` Task 18 complete-and-consistent verdict grammar; `requirements/review.md` task-feedback and whole-branch verdict definitions; `skills/hamilton-code-feedback/SKILL.md` and `skills/hamilton-review/SKILL.md` verdict contracts)

- [`skills/hamilton-code/SKILL.md:31`; `skills/hamilton-plan/SKILL.md:195`; `tests/skills/execution-contracts.test.ts:28`] [P1] The shared helper now recognizes only the exact canonical abandonment suffix, but both execution producers still tell agents to exclude any heading whose suffix merely begins with the literal `(abandoned`, and the skill test explicitly locks that broader rule. A malformed heading such as `(abandoned - not canonical)` is active to every helper yet abandoned to plan/code, recreating divergent task identity and a generic alternate grammar after the approved bootstrap exception. Make both skills require the complete canonical `### Task N: <title> (abandoned — <reason>)` suffix, remove the starts-with rule, and test that malformed markers remain active consistently with the shared parser. (violates: `plan.md` Task 17 exact active-task resolution; `requirements/execution.md` active-task and canonical abandonment contract; approved self-hosting exception's no-compatibility boundary)

### Suggestions

- None.
